% !TEX program = xelatex
% Biên dịch: xelatex main.tex (chạy 2 lần để cập nhật mục lục & tham chiếu)
%
% Báo cáo hoàn chỉnh, gộp từ ba phần đã biên soạn riêng:
%   docs/algorithms_theory   -> Chương 1 (Cơ sở lý thuyết và phương pháp hỗ trợ)
%   docs/eda_report          -> Chương 2 (Phân tích khám phá dữ liệu)
%   docs/build_model_report  -> Chương 3 (Xây dựng và so sánh mô hình)
% Hình và bảng được sao chép nguyên trạng từ ba thư mục trên; xem các
% gen_tables.py / gen_figures.py tương ứng ở đó nếu cần tái tạo.
\RequirePackage{fix-cm}
\documentclass[a4paper]{report}

% ---------- Cỡ chữ 13pt thật sự (lớp report chuẩn chỉ hỗ trợ 10/11/12pt,
% nên option [13pt] bị bỏ qua; gói fontsize cho phép đặt cỡ tuỳ ý) ----------
\usepackage[fontsize=13pt]{fontsize}

% ---------- Ngôn ngữ & font (XeLaTeX) ----------
\usepackage{fontspec}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{unicode-math}
\usepackage{polyglossia}
\setdefaultlanguage{vietnamese}
% Font Latin Modern (font TeX cổ điển) — giống bài mẫu, có hỗ trợ tiếng Việt
\setmainfont{Latin Modern Roman}
\setmathfont{Latin Modern Math}
% LM Mono không có bản bold → ánh xạ bold về regular để tránh cảnh báo font
\setmonofont{Latin Modern Mono}[BoldFont={Latin Modern Mono}]

% ---------- Trình bày trang ----------
\usepackage[a4paper,left=3.5cm,right=2cm,top=2.5cm,bottom=2.5cm]{geometry}
\usepackage{setspace}
\onehalfspacing
% Cho phép TeX nới dòng ở bước cuối để tránh text tràn lề (overfull hbox)
\setlength{\emergencystretch}{3em}
% Bỏ qua các cảnh báo underfull nhẹ do tiếng Việt và tên vùng dài trong danh sách.
\hbadness=2000

% ---------- Hình, bảng, biểu ----------
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{float}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage{makecell}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{subcaption}
\usepackage[table]{xcolor}

% ---------- Sơ đồ minh hoạ ----------
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,calc}

% ---------- Liên kết ----------
\usepackage[hidelinks]{hyperref}
\usepackage{enumitem}

% \code: định danh dạng monospace; cho phép ngắt dòng ngay sau dấu gạch dưới
% để các tên biến dài (vd. num_of_prev_attempts) không tràn ra lề.
\newcommand{\code}[1]{{\ttfamily\def\_{\textunderscore\discretionary{}{}{}}#1}}
\pdfstringdefDisableCommands{%
  \def\code#1{#1}%
  \def\_{ }%
}

\begin{document}

% =====================================================================
%  TRANG BÌA
% =====================================================================
\begin{titlepage}
  \centering
  {\large\bfseries TRƯỜNG ĐẠI HỌC QUY NHƠN\\[3pt]
   KHOA TOÁN \& THỐNG KÊ\par}

  \vspace{1.3cm}

  % ----- LOGO trường -----
  \includegraphics[width=3.5cm]{logo_qnu.png}\par

  \vspace{1.4cm}

  {\bfseries TIỂU LUẬN MÔN HỌC:\\[3pt]
   PHÂN TÍCH DỮ LIỆU KHOA HỌC CHUYÊN NGÀNH\par}

  \vspace{0.5cm}
  \rule{\textwidth}{0.4pt}\\[0.55cm]
  {\Large\bfseries CẢNH BÁO SỚM RỦI RO HỌC TẬP\\[5pt]
   TỪ DỮ LIỆU TƯƠNG TÁC TRỰC TUYẾN:\\[5pt]
   XÂY DỰNG, SO SÁNH VÀ DIỄN GIẢI\\[5pt]
   CÁC MÔ HÌNH HỌC MÁY\\[5pt]
   TRÊN BỘ DỮ LIỆU OULAD\par}
  \vspace{0.5cm}
  \rule{\textwidth}{0.4pt}

  \vspace{1.8cm}

  {\normalsize
  \begin{tabular}{r@{\hspace{0.5em}}l}
    \itshape Trình độ đào tạo:      & Thạc sĩ \\[5pt]
    \itshape Học viên thực hiện:    & Nguyễn Hồ Bảo Thiên \\[5pt]
    \itshape Mã số sinh viên:       & 8282548017 \\[5pt]
    \itshape Lớp:                   & Khoa học dữ liệu K28B \\[5pt]
    \itshape Giảng viên hướng dẫn:  & TS. Hồ Văn Lâm \\
  \end{tabular}\par}

  \vfill
  {\itshape Gia Lai, 2026\par}
\end{titlepage}

% =====================================================================
%  PHẦN ĐẦU (mục lục, danh mục hình/bảng) — đánh số La Mã
% =====================================================================
\pagenumbering{roman}
\tableofcontents
\listoffigures
\listoftables
\clearpage

% =====================================================================
%  NỘI DUNG CHÍNH — đánh số Ả Rập
% =====================================================================
\pagenumbering{arabic}

\chapter*{Mở đầu}
\addcontentsline{toc}{chapter}{Mở đầu}

Sự phát triển mạnh mẽ của hình thức học trực tuyến và học kết hợp trong những năm gần đây
đã tạo ra lượng lớn dữ liệu phản ánh quá trình học tập và tương tác của sinh viên trên các hệ thống
quản lý học tập. Nguồn dữ liệu này không chỉ ghi nhận kết quả học tập mà còn phản ánh nhiều khía cạnh khác
như mức độ tham gia, tần suất truy cập, thời gian học tập và hành vi tương tác với tài nguyên học tập.
Điều đó mở ra cơ hội ứng dụng các phương pháp học máy và phân tích dữ liệu nhằm khai thác những thông tin
tiềm ẩn, từ đó hỗ trợ quá trình ra quyết định trong lĩnh vực giáo dục.

Trong số các ứng dụng của khai phá dữ liệu giáo dục, bài toán phát hiện sớm sinh viên có nguy cơ
học tập kém, trượt môn hoặc bỏ học nhận được nhiều sự quan tâm do có ý nghĩa thực tiễn cao.
Nếu được nhận diện ở giai đoạn đầu của học phần, các sinh viên này có thể nhận được những biện pháp hỗ trợ
và can thiệp phù hợp từ giảng viên hoặc cố vấn học tập, góp phần cải thiện kết quả học tập, giảm tỷ lệ bỏ học
và nâng cao chất lượng đào tạo.

Trong dự án này, dữ liệu được sử dụng là bộ \textbf{Open University Learning
Analytics Dataset}, viết tắt OULAD, do The Open University tại Vương quốc Anh
công bố tháng 12 năm 2015. Bộ dữ liệu ghi lại thông tin về các học phần, sinh
viên và toàn bộ tương tác của họ với hệ thống học tập trực tuyến VLE trên bảy
học phần được lựa chọn.

Toàn bộ nghiên cứu được tổ chức thành ba chương kế tiếp nhau:

\begin{enumerate}[leftmargin=*]
  \item \textbf{Cơ sở lý thuyết}: trình bày nguyên lý hoạt động và công thức
        toán học cốt lõi của bốn thuật toán học máy được sử dụng trong nghiên
        cứu --- Logistic Regression, Random Forest, CatBoost và LightGBM ---
        đồng thời bổ sung cơ sở cho hai phương pháp hỗ trợ thực nghiệm:
        Bayesian Optimization trong tinh chỉnh siêu tham số và SHAP trong diễn
        giải mô hình.
  \item \textbf{Phân tích khám phá dữ liệu}: tìm hiểu cấu trúc, quy mô và
        chất lượng của bộ dữ liệu OULAD; xây dựng tập dữ liệu dạng
        \textit{panel} theo nhiều mốc snapshot phù hợp với bài
        toán dự báo sớm; khám phá mối quan hệ giữa các đặc trưng nhân khẩu
        học, hành vi học tập và kết quả cuối khoá.
  \item \textbf{Xây dựng và so sánh mô hình}: tiền xử lý dữ liệu, khảo sát sơ
        bộ hiệu suất theo thời gian, huấn luyện và tinh chỉnh bốn thuật toán
        bằng Bayesian Optimization kết hợp kỹ thuật xử lý mất cân bằng nhãn,
        so sánh hiệu suất, đánh giá độ ổn định khi rút gọn đặc trưng, hiệu
        chỉnh ngưỡng phân loại và diễn giải mô hình bằng SHAP.
\end{enumerate}

% =====================================================================
%  CHƯƠNG 1 — CƠ SỞ LÝ THUYẾT VÀ PHƯƠNG PHÁP HỖ TRỢ
% =====================================================================
\chapter{Cơ sở lý thuyết}
\label{part:theory}

\section{Tổng quan nội dung lý thuyết}

Chương này trình bày nền tảng lý thuyết cho toàn bộ quy trình thực nghiệm ở
các chương sau. Nội dung không chỉ bao gồm bốn thuật toán học máy có giám sát
được dùng để dự báo rủi ro học tập, mà còn bao gồm hai phương pháp hỗ trợ quan
trọng: Bayesian Optimization để tinh chỉnh siêu tham số và SHAP để diễn giải
dự báo của mô hình.

Bốn thuật toán được đưa vào so sánh gồm Logistic Regression, Random Forest,
CatBoost và LightGBM. Các thuật toán này đại diện cho ba hướng tiếp cận tiêu
biểu trong học máy có giám sát:

\begin{itemize}[leftmargin=*]
  \item \textbf{Mô hình tuyến tính}: Logistic Regression, đơn giản, dễ diễn
        giải và được sử dụng làm baseline.
  \item \textbf{Học theo phương pháp bagging}: Random Forest, kết hợp nhiều cây
        quyết định được huấn luyện độc lập nhằm giảm phương sai và cải thiện
        khả năng tổng quát hóa.
  \item \textbf{Học theo phương pháp boosting}: CatBoost và LightGBM, xây dựng
        tuần tự các mô hình yếu, trong đó mỗi mô hình kế tiếp tập trung khắc
        phục sai số của các mô hình trước.
\end{itemize}

Đối với mỗi thuật toán, các mục tiếp theo trình bày nguyên lý hoạt động, công
thức toán học cốt lõi và những cơ chế đặc trưng trong bối cảnh bài toán cảnh
báo sớm rủi ro học tập. Sau phần thuật toán, chương tiếp tục trình bày
Bayesian Optimization và SHAP để làm rõ cơ sở lý thuyết cho hai bước quan
trọng ở Chương~\ref{part:model}: tinh chỉnh mô hình và diễn giải kết quả.

\section{Logistic Regression}

\subsection{Mô hình xác suất}

Logistic Regression là một mô hình tuyến tính suy rộng cho bài toán phân loại
nhị phân. Với vector đặc trưng đầu vào
$\mathbf{x} \in \mathbb{R}^p$, mô hình trước tiên tính một tổ hợp tuyến tính:

\[
  z = \mathbf{w}^\top \mathbf{x} + b,
\]

trong đó $\mathbf{w} \in \mathbb{R}^p$ là vector trọng số và $b$ là hệ số chặn.
Giá trị $z$ sau đó được ánh xạ về khoảng $(0, 1)$ thông qua \textbf{hàm
sigmoid}:

\[
  \hat{p} = \sigma(z) = \frac{1}{1 + e^{-z}},
\]

được diễn giải là xác suất dự báo của lớp dương. Quyết định
phân loại cuối cùng dựa trên ngưỡng mặc định $\tau = 0.5$:
$\hat{y} = 1$ nếu $\hat{p} \ge \tau$, ngược lại $\hat{y} = 0$.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{sigmoid_logloss.png}
  \caption{Hàm sigmoid (trái) và hàm mất mát Binary Cross-Entropy theo xác
  suất dự báo, ứng với hai trường hợp nhãn thật $y=1$ và $y=0$ (phải).}
  \label{fig:sigmoid}
\end{figure}

Như minh hoạ ở Hình~\ref{fig:sigmoid} (trái), hàm sigmoid có dạng chữ S, đối
xứng qua điểm $(0, 0.5)$, bão hoà dần về 0 và 1 ở hai đầu --- đây chính là cơ
chế giúp mô hình tuyến tính $z$ (có thể nhận giá trị bất kỳ trên
$\mathbb{R}$) được chuyển thành một xác suất hợp lệ.

\subsection{Hàm mất mát, regularization và trọng số lớp}

Tham số $(\mathbf{w}, b)$ được ước lượng bằng phương pháp \textbf{hợp lý cực
đại}, tương đương với việc tối thiểu hoá hàm mất mát
\textbf{Binary Cross-Entropy}, hay log-loss, trên tập huấn luyện gồm $N$ mẫu:

\[
  \mathcal{L}(\mathbf{w}, b) = -\frac{1}{N} \sum_{i=1}^{N}
  \Big[ y_i \log(\hat{p}_i) + (1 - y_i) \log(1 - \hat{p}_i) \Big].
\]

Hình~\ref{fig:sigmoid} (phải) minh hoạ trực quan hàm mất mát này: khi nhãn
thật $y_i = 1$, mất mát giảm dần về 0 khi $\hat{p}_i \to 1$ nhưng tiến tới vô
cực khi $\hat{p}_i \to 0$ --- tức mô hình bị phạt rất nặng nếu dự báo sai với
độ tin cậy cao. Vì $\mathcal{L}$ là hàm lồi theo $(\mathbf{w}, b)$, việc tối ưu
thường dùng các phương pháp lặp dựa trên gradient (gradient descent hoặc các
biến thể tựa Newton như L-BFGS), với đạo hàm riêng theo $\mathbf{w}$ có dạng
tường minh:

\[
  \nabla_{\mathbf{w}} \mathcal{L} = \frac{1}{N} \mathbf{X}^\top
  (\hat{\mathbf{p}} - \mathbf{y}).
\]

\label{sec:logreg_reg}
\noindent\textbf{Regularization.} Để hạn chế hiện tượng overfitting khi số đặc trưng lớn hoặc các đặc
trưng có tương quan mạnh, một số hạng regularization được cộng thêm vào hàm
mục tiêu. Với regularization $L_2$/Ridge, bài toán tối ưu trở thành:

\[
  \min_{\mathbf{w}, b} \; \frac{1}{2} \lVert \mathbf{w} \rVert_2^2
  + C \sum_{i=1}^{N} \ell(y_i, \hat{p}_i),
\]

trong đó $\ell$ là hàm mất mát log-loss của từng mẫu, và $C > 0$ là siêu tham
số \textbf{nghịch đảo} của regularization: $C$ càng nhỏ, mô hình bị ràng
buộc càng chặt, trọng số $\mathbf{w}$ bị co về gần 0, giảm phương sai nhưng có
thể tăng bias; $C$ càng lớn, mô hình càng tự do khớp dữ liệu huấn
luyện. 

\label{sec:logreg_weight}
\noindent\textbf{Trọng số lớp cho dữ liệu mất cân bằng.} Với bài toán mất cân bằng nhãn, hàm mất mát có thể được điều chỉnh bằng cách
gán trọng số khác nhau cho từng lớp:

\[
  \mathcal{L}_w(\mathbf{w}, b) = -\frac{1}{N} \sum_{i=1}^{N} w_{y_i}
  \Big[ y_i \log(\hat{p}_i) + (1 - y_i) \log(1 - \hat{p}_i) \Big],
\]

trong đó $w_{y_i}$ là trọng số ứng với lớp của mẫu $i$. Việc tăng $w_1$ khiến mô hình phải trả giá
mất mát lớn hơn khi phân loại sai một sinh viên At-risk, làm mô hình dịch
chuyển ngưỡng quyết định để bắt được nhiều trường hợp At-risk hơn.

\section{Decision Tree và Random Forest}

\subsection{Decision Tree}

Decision Tree trong nghiên cứu này được xây dựng theo thuật toán CART. Thuật
toán hoạt động bằng cách chia đệ quy không gian đặc trưng thành các vùng ngày
càng nhỏ, trong đó mỗi vùng tương ứng với một \textbf{lá} và đưa ra một dự báo
cụ thể. Tại mỗi nút, thuật
toán lựa chọn một đặc trưng $j$ và một ngưỡng $s$ sao cho phép phân chia dữ
liệu thành hai nhánh $\{\mathbf{x}:x_j\le s\}$ và
$\{\mathbf{x}:x_j>s\}$ làm giảm độ đo \textbf{tạp chất} nhiều nhất.
Đối với bài toán phân loại, độ đo được sử dụng phổ biến là chỉ số
\textbf{Gini}:

\[
  \text{Gini}(t) = 1 - \sum_{k} p_k(t)^2,
\]

trong đó $p_k(t)$ là tỷ lệ mẫu thuộc lớp $k$ tại nút $t$. Chỉ số Gini bằng 0
khi nút $t$ hoàn toàn thuần nhất (chỉ chứa các mẫu thuộc cùng một lớp), và đạt
giá trị lớn nhất khi các lớp được phân bố đồng đều. Độ giảm tạp chất khi chia
nút $t$ thành hai nút con $t_L, t_R$ được tính như sau:

\[
  \Delta \text{Gini} = \text{Gini}(t) - \frac{N_L}{N_t}\text{Gini}(t_L)
  - \frac{N_R}{N_t}\text{Gini}(t_R),
\]

Thuật toán sẽ lựa chọn cặp $(j,s)$ sao cho $\Delta \text{Gini}$ đạt giá trị lớn
nhất tại mỗi nút. Quá trình phân chia kết thúc khi thỏa mãn một trong các điều
kiện dừng, chẳng hạn như đạt độ sâu tối đa, số lượng mẫu tối thiểu tại mỗi lá
hoặc độ tạp chất bằng 0.

Cây quyết định đơn lẻ có ưu điểm là cấu trúc trực quan, dễ diễn giải và có khả
năng xử lý hiệu quả cả đặc trưng số lẫn đặc trưng phân loại. Tuy nhiên, mô hình
này có \textbf{phương sai cao}: chỉ cần những thay đổi nhỏ trong dữ liệu huấn
luyện cũng có thể tạo ra một cấu trúc cây khác biệt đáng kể. Khi cây phát triển
quá sâu, mô hình dễ dẫn đến hiện tượng overfitting, làm
giảm khả năng tổng quát hóa trên dữ liệu chưa quan sát.

\subsection{Bagging}

Để khắc phục nhược điểm về phương sai của cây quyết định, phương pháp
\textbf{bagging} xây dựng nhiều cây quyết định độc lập
trên các tập dữ liệu bootstrap được lấy mẫu lại từ tập huấn luyện ban đầu. Dự
báo cuối cùng được xác định bằng cách tổng hợp kết quả từ toàn bộ các cây.

\begin{figure}[H]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      node distance=6mm and 9mm,
      box/.style={draw, rounded corners, align=center, font=\small, minimum width=2.1cm, minimum height=0.9cm},
      >={Stealth[length=2mm]}
    ]
    \node[box, fill=gray!10] (D) {Tập dữ liệu\\huấn luyện $D$};

    \node[box, fill=blue!8, right=of D, yshift=2.4cm] (b1) {Mẫu bootstrap\\$D_1$};
    \node[box, fill=blue!8, right=of D] (b2) {Mẫu bootstrap\\$D_2$};
    \node[box, fill=blue!8, right=of D, yshift=-2.4cm] (b3) {Mẫu bootstrap\\$D_B$};

    \node[box, fill=green!10, right=of b1] (t1) {Cây $T_1$};
    \node[box, fill=green!10, right=of b2] (t2) {Cây $T_2$};
    \node[box, fill=green!10, right=of b3] (t3) {Cây $T_B$};

    \node[box, fill=orange!15, right=18mm of t2] (agg) {Tổng hợp\\vote / trung bình};

    \draw[->] (D) -- (b1);
    \draw[->] (D) -- (b2);
    \draw[->] (D) -- (b3);
    \draw[->] (b1) -- (t1);
    \draw[->] (b2) -- (t2);
    \draw[->] (b3) -- (t3);
    \draw[->] (t1) -- (agg);
    \draw[->] (t2) -- (agg);
    \draw[->] (t3) -- (agg);

    \node[right=6mm of agg, align=left, font=\small] (out) {Dự báo\\cuối cùng};
    \draw[->] (agg) -- (out);
  \end{tikzpicture}}
  \caption{Sơ đồ nguyên lý Bagging: $B$ cây được huấn luyện độc lập trên các
  mẫu bootstrap khác nhau, kết quả được tổng hợp bằng biểu quyết đa số cho
  phân loại hoặc trung bình cho hồi quy.}
  \label{fig:bagging}
\end{figure}

Mỗi mẫu bootstrap $D_b$ ($b = 1, \ldots, B$) có cùng kích thước với tập dữ liệu
gốc $D$ nhưng được lấy mẫu \textbf{có hoàn lại}. Do đó, trung bình khoảng
63.2\% số mẫu gốc xuất hiện trong mỗi tập bootstrap, trong khi một số mẫu có
thể được chọn nhiều lần và một số mẫu khác không được chọn. Đối với bài toán
phân loại, dự báo cuối cùng được xác định bằng lớp xuất hiện nhiều nhất trong
$B$ dự báo:

\[
  \hat{y}(\mathbf{x}) = \text{mode}\big\{ T_1(\mathbf{x}), T_2(\mathbf{x}),
  \ldots, T_B(\mathbf{x}) \big\}.
\]

Do mỗi cây được huấn luyện trên một mẫu bootstrap khác nhau nên sai số giữa các
cây thường không hoàn toàn tương quan. Việc kết hợp dự báo từ nhiều cây giúp
giảm ảnh hưởng của nhiễu ngẫu nhiên, từ đó làm giảm phương sai của mô hình tổng
thể trong khi chỉ làm tăng độ chệch ở mức không đáng kể.

\subsection{Random Forest và độ quan trọng đặc trưng}

Random Forest được xây dựng dựa trên phương pháp bagging nhưng bổ sung thêm
một cơ chế lựa chọn đặc trưng ngẫu nhiên trong quá trình xây dựng từng cây
quyết định. Cụ thể, tại mỗi nút của mỗi cây, thay vì xem xét toàn bộ $p$ đặc
trưng để tìm phép phân chia tối ưu, thuật toán chỉ lựa chọn ngẫu nhiên một tập
con gồm $m < p$ đặc trưng (thường $m \approx \sqrt{p}$ đối với bài toán phân
loại), sau đó tìm phép phân chia tốt nhất trên tập đặc trưng này.

Việc mỗi cây được xây dựng từ những tập đặc trưng khác nhau làm giảm mức độ
tương đồng giữa các cây quyết định. Nếu tất cả các cây đều luôn lựa chọn những
đặc trưng có khả năng phân chia tốt nhất tại các nút gần gốc, cấu trúc của
chúng sẽ trở nên rất giống nhau và hiệu quả của phương pháp bagging bị hạn chế.
Bằng cách giới hạn số đặc trưng được xem xét tại mỗi nút, Random Forest tạo ra
những cây đa dạng hơn, từ đó giảm phương sai của mô hình và cải thiện khả năng
tổng quát hóa trên dữ liệu chưa quan sát.

\label{sec:rf_importance}

Ngoài khả năng dự báo, Random Forest còn cung cấp một thước đo để đánh giá mức
độ đóng góp của từng đặc trưng trong quá trình xây dựng mô hình. Một trong
những thước đo được sử dụng phổ biến là \textit{Mean Decrease in Impurity},
viết tắt MDI, được tính dựa trên tổng mức giảm chỉ số Gini tại các nút sử dụng
đặc trưng đó để phân chia dữ liệu:

\[
  \text{Importance}(x_j)=\frac{1}{B}\sum_{b=1}^{B}
  \sum_{t\in T_b:\,\text{split trên }x_j}
  \frac{N_t}{N}\Delta\text{Gini}(t).
\]

Trong đó, $\Delta\text{Gini}(t)$ là mức giảm chỉ số Gini tại nút $t$, $N_t$ là
số mẫu đi qua nút đó và $N$ là tổng số mẫu trong tập huấn luyện. Giá trị
Importance của một đặc trưng được tính bằng tổng mức giảm Gini do đặc trưng đó
tạo ra trên toàn bộ các cây trong rừng, có xét đến tỷ lệ mẫu đi qua từng nút,
sau đó lấy trung bình trên $B$ cây. Đặc trưng có giá trị Importance càng lớn
được xem là càng đóng góp nhiều vào khả năng phân loại của mô hình.

\section{Gradient Boosting và CatBoost}
\label{sec:catboost}

\subsection{Nguyên lý Gradient Boosting}

Gradient Boosting là một phương pháp học tập hợp xây dựng mô hình theo cách
\textbf{tuần tự}. Khác với bagging, trong đó các cây quyết định được huấn luyện
độc lập rồi kết hợp kết quả dự báo, Gradient Boosting xây dựng từng cây mới dựa
trên những sai số mà mô hình hiện tại vẫn chưa khắc phục được. Sau mỗi vòng
lặp, mô hình được cập nhật theo công thức

\[
  F_m(\mathbf{x}) = F_{m-1}(\mathbf{x}) + \eta\,h_m(\mathbf{x}),
\]

trong đó $\eta \in (0,1]$ là learning rate và $h_m$ là một cây quyết định yếu,
thường có độ sâu nhỏ. Thay vì dự báo trực tiếp nhãn, cây
$h_m$ được huấn luyện để xấp xỉ \textbf{gradient âm} của hàm mất mát tại mô
hình hiện tại $F_{m-1}$. Đại lượng này được gọi là \textbf{giả phần dư}
pseudo-residual:

\[
  r_i^{(m)}
  =
  -
  \left[
    \frac{\partial\,\ell(y_i,F(\mathbf{x}_i))}
    {\partial\,F(\mathbf{x}_i)}
  \right]_{F=F_{m-1}}.
\]

Đối với hàm mất mát log-loss trong bài toán phân loại nhị phân, giả phần dư có
dạng

\[
r_i^{(m)} = y_i - \hat{p}_i^{(m-1)},
\]

tức là sai khác giữa nhãn thực tế và xác suất dự báo của mô hình ở vòng trước.
Sau khi cây $h_m$ được huấn luyện để dự báo các giả phần dư này, dự báo của cây
được cộng vào mô hình hiện tại nhằm giảm giá trị của hàm mất mát. Quá trình
được lặp lại nhiều lần cho đến khi đạt số vòng boosting hoặc thỏa mãn tiêu chí
dừng.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      node distance=5mm,
      box/.style={draw, rounded corners, align=center, font=\small, minimum height=0.9cm},
      >={Stealth[length=2mm]}
    ]
    \node[box, fill=gray!10, minimum width=1.7cm] (f0) {$F_0$\\(hằng số)};
    \node[box, fill=blue!8, right=of f0, minimum width=1.7cm] (h1) {$h_1$};
    \node[box, fill=gray!10, right=of h1, minimum width=1.7cm] (f1) {$F_1$};
    \node[box, fill=blue!8, right=of f1, minimum width=1.7cm] (h2) {$h_2$};
    \node[box, fill=gray!10, right=of h2, minimum width=1.7cm] (f2) {$F_2$};
    \node[right=4mm of f2, font=\small] (dots) {$\cdots$};
    \node[box, fill=gray!10, right=8mm of dots, minimum width=1.9cm] (fm) {$F_M$};

    \draw[->] (f0) -- node[above, font=\scriptsize] {residual $r^{(1)}$} (h1);
    \draw[->] (h1) -- node[above, font=\scriptsize] {$+\,\eta h_1$} (f1);
    \draw[->] (f1) -- node[above, font=\scriptsize] {residual $r^{(2)}$} (h2);
    \draw[->] (h2) -- node[above, font=\scriptsize] {$+\,\eta h_2$} (f2);
    \draw[->] (f2) -- (dots);
    \draw[->] (dots) -- (fm);
  \end{tikzpicture}
  \caption{Sơ đồ nguyên lý Gradient Boosting. Ở mỗi vòng lặp, một cây quyết
  định yếu được huấn luyện để dự báo giả phần dư của mô hình hiện tại, sau đó
  được cộng vào mô hình với hệ số học $\eta$.}
  \label{fig:boosting}
\end{figure}

CatBoost và LightGBM đều thuộc họ \textbf{Gradient Boosted Decision Trees},
viết tắt GBDT, trong đó cây quyết định được sử dụng làm bộ học yếu. Hai thuật toán đều
dựa trên nguyên lý Gradient Boosting nhưng khác nhau ở cách xây dựng cây và xử
lý dữ liệu nhằm cải thiện độ chính xác cũng như khả năng tổng quát hóa của mô
hình.

\subsection{Ordered Boosting và Ordered Target Statistics}

Một hạn chế của Gradient Boosting truyền thống là hiện tượng
\textbf{prediction shift}. Trong quá trình huấn luyện, giả phần dư của một mẫu
được tính từ mô hình đã từng sử dụng chính mẫu đó ở các vòng boosting trước.
Điều này khiến sai số ước lượng trở nên lạc quan hơn so với khi mô hình được áp
dụng trên dữ liệu mới, từ đó làm giảm khả năng tổng quát hóa.

Để khắc phục hiện tượng này, CatBoost sử dụng cơ chế
\textbf{Ordered Boosting}. Trước tiên, dữ liệu được sắp xếp theo một hoán vị
ngẫu nhiên. Khi tính giả phần dư của một mẫu, mô hình chỉ được phép sử dụng
thông tin từ các mẫu xuất hiện trước mẫu đó trong hoán vị. Nhờ vậy, dự báo của
mỗi mẫu luôn được tạo ra bởi một mô hình chưa từng quan sát nhãn của chính mẫu
đó, giúp giảm hiện tượng prediction shift và hạn chế rò rỉ thông tin trong quá
trình huấn luyện.

\medskip
\noindent\textbf{Ordered Target Statistics cho đặc trưng phân loại.}

Đối với các đặc trưng phân loại có nhiều mức giá trị, CatBoost biểu diễn mỗi
mức giá trị bằng một \textbf{thống kê mục tiêu}, phản ánh
mối quan hệ giữa mức giá trị đó và biến mục tiêu. Để tránh rò rỉ thông tin,
thống kê này cũng được tính theo nguyên tắc \textit{ordered}, nghĩa là chỉ sử
dụng các mẫu xuất hiện trước trong cùng một hoán vị ngẫu nhiên:

\[
  \text{TS}(\mathbf{x}_i^{(k)}) =
  \frac{\displaystyle
  \sum_{j:\pi(j)<\pi(i),\,x_j^{(k)}=x_i^{(k)}}y_j
  + aP}
  {\displaystyle
  \sum_{j:\pi(j)<\pi(i),\,x_j^{(k)}=x_i^{(k)}}1
  + a}.
\]

Trong đó, $x_i^{(k)}$ là giá trị của đặc trưng phân loại thứ $k$ tại mẫu $i$,
$P$ là giá trị tiên nghiệm của biến mục tiêu và $a>0$ là hệ số làm trơn. Thành
phần làm trơn giúp hạn chế các ước lượng cực đoan khi một
mức giá trị chỉ xuất hiện ở rất ít mẫu.

\subsection{Cây đối xứng}

CatBoost sử dụng \textbf{cây đối xứng} làm bộ học yếu. Trong loại cây này, tất
cả các nút nằm trên cùng một tầng đều sử dụng cùng một đặc
trưng và cùng một ngưỡng để phân chia dữ liệu. Nhờ cấu trúc đối xứng, đường đi
từ nút gốc đến một lá chỉ phụ thuộc vào chuỗi kết quả của các phép so sánh tại
từng tầng của cây.

So với cây quyết định thông thường, cây đối xứng có cấu trúc đơn giản và đồng
nhất hơn, giúp tăng tốc độ dự báo cũng như giảm chi phí tính toán. Đồng thời,
việc hạn chế mức độ phức tạp của cây cũng góp phần giảm nguy cơ quá khớp và cải
thiện khả năng tổng quát hóa của mô hình.

\section{LightGBM}

LightGBM là một thuật toán thuộc họ GBDT,
được thiết kế nhằm nâng cao hiệu quả huấn luyện trên các tập dữ liệu có kích
thước lớn. So với GBDT truyền thống, LightGBM tập trung tối ưu tốc độ huấn
luyện và mức sử dụng bộ nhớ thông qua ba cải tiến chính: chiến lược xây dựng
cây, phương pháp lấy mẫu dữ liệu và kỹ thuật giảm số lượng đặc trưng cần xử lý.

\subsection{Tăng trưởng cây theo lá}

Phần lớn các thuật toán GBDT xây dựng cây theo chiến lược
\textbf{level-wise}, còn gọi là depth-wise, trong đó tất cả các lá ở cùng một tầng
được mở rộng trước khi chuyển sang tầng tiếp theo. Cách xây dựng này tạo ra các
cây có cấu trúc tương đối cân bằng.

Ngược lại, LightGBM sử dụng chiến lược \textbf{leaf-wise}, hay
\textit{best-first}. Ở mỗi bước, thuật toán chỉ mở rộng một lá duy nhất, đó là
lá mang lại mức giảm hàm mất mát lớn nhất trong số tất cả các lá hiện có, bất
kể lá đó nằm ở độ sâu nào của cây.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.47\textwidth}
    \centering
    \begin{tikzpicture}[
        level distance=11mm, sibling distance=13mm,
        every node/.style={circle, draw, fill=blue!10, minimum size=5.5mm, font=\scriptsize}
      ]
      \node {}
        child { node {}
          child { node {} }
          child { node {} }
        }
        child { node {}
          child { node {} }
          child { node {} }
        };
    \end{tikzpicture}
    \caption{Level-wise: mở rộng đồng thời tất cả các lá trên cùng một tầng.}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.47\textwidth}
    \centering
    \begin{tikzpicture}[
        level distance=11mm,
        every node/.style={circle, draw, fill=orange!15, minimum size=5.5mm, font=\scriptsize}
      ]
      \node {}
        child { node {}
          child { node {}
            child { node {} }
            child { node {} }
          }
          child { node {} }
        }
        child { node {} };
    \end{tikzpicture}
    \caption{Leaf-wise: luôn mở rộng lá mang lại mức giảm mất mát lớn nhất.}
  \end{subfigure}
  \caption{So sánh chiến lược xây dựng cây level-wise và leaf-wise với cùng số
  lượng lá.}
  \label{fig:growth}
\end{figure}

Như minh họa ở Hình~\ref{fig:growth}, với cùng số lượng lá, chiến lược
leaf-wise thường đạt giá trị hàm mất mát thấp hơn do luôn ưu tiên mở rộng lá có
khả năng cải thiện mô hình nhiều nhất. Tuy nhiên, cách xây dựng này cũng tạo ra
những cây sâu và mất cân bằng hơn, làm tăng nguy cơ quá khớp trên các tập dữ
liệu nhỏ. Vì vậy, LightGBM thường kết hợp các siêu tham số như
\code{num\_leaves}, \code{max\_depth} và \code{min\_child\_samples} để kiểm
soát độ phức tạp của cây.

\subsection{Tối ưu hiệu quả huấn luyện: GOSS và EFB}

Trong Gradient Boosting, các mẫu có giá trị gradient lớn thường là những mẫu
mà mô hình dự báo chưa tốt, do đó chứa nhiều thông tin hơn cho việc xây dựng
cây ở vòng boosting tiếp theo. Ngược lại, các mẫu có gradient nhỏ thường đã
được mô hình dự báo tương đối chính xác và đóng góp ít hơn vào quá trình tìm
điểm phân chia.

Dựa trên nhận xét này, GOSS giảm số lượng
mẫu cần xử lý bằng cách:

\begin{itemize}[leftmargin=*]
  \item Giữ lại toàn bộ $a\%$ số mẫu có giá trị gradient lớn nhất.
  \item Lấy mẫu ngẫu nhiên $b\%$ trong nhóm các mẫu còn lại.
  \item Khi tính toán độ giảm hàm mất mát, các mẫu được lấy ngẫu nhiên sẽ được
        gán trọng số $\frac{1-a}{b}$ nhằm bù lại ảnh hưởng của các mẫu đã bị
        loại bỏ.
\end{itemize}

Nhờ chỉ xử lý một phần dữ liệu nhưng vẫn giữ lại hầu hết các mẫu quan trọng,
GOSS giúp giảm đáng kể thời gian huấn luyện trong khi chỉ ảnh hưởng rất nhỏ đến
chất lượng của điểm phân chia được lựa chọn.

\medskip
\noindent\textbf{Exclusive Feature Bundling.}

Đối với các tập dữ liệu có số lượng lớn đặc trưng thưa,
nhiều đặc trưng gần như không đồng thời nhận giá trị khác 0, chẳng hạn các đặc
trưng được tạo ra từ one-hot encoding. LightGBM khai thác đặc điểm này thông
qua kỹ thuật EFB.

Ý tưởng của EFB là gộp nhiều đặc trưng gần như loại trừ lẫn nhau thành một đặc
trưng mới mà vẫn bảo toàn thông tin của từng đặc trưng ban đầu. Để thực hiện
điều này, mỗi đặc trưng được ánh xạ vào một khoảng giá trị riêng trong đặc
trưng tổng hợp. Việc xác định những đặc trưng nào có thể gộp chung được mô hình
hóa thành bài toán tô màu đồ thị trên đồ thị xung đột giữa các
đặc trưng và được giải bằng thuật toán tham lam.

Nhờ giảm số lượng đặc trưng cần xem xét khi xây dựng histogram để tìm điểm
phân chia, EFB giúp rút ngắn thời gian huấn luyện và giảm yêu cầu bộ nhớ, đồng
thời vẫn duy trì độ chính xác của mô hình trong hầu hết các trường hợp.

\section{Bayesian Optimization}
\label{sec:bayes_opt}

Trong các mô hình học máy, hiệu suất dự báo phụ thuộc đáng kể vào lựa chọn
\textbf{siêu tham số}, chẳng hạn hệ số regularization của
Logistic Regression, số cây và độ sâu của Random Forest, learning rate của
CatBoost hoặc số lá của LightGBM. 

\textbf{Bayesian Optimization} là một phương pháp tối ưu hóa phù hợp cho các
bài toán trong đó việc đánh giá hàm mục tiêu có chi phí tính toán cao và không
thể biểu diễn trực tiếp dưới dạng công thức hoặc đạo hàm. Trong bài toán tinh
chỉnh siêu tham số mô hình học máy, hàm mục tiêu thường là một chỉ số đánh giá
được tính trên tập validation hoặc thông qua cross-validation, chẳng hạn như
F1-score của lớp At-risk.

Khác với các phương pháp tìm kiếm đơn giản như Grid Search hoặc Random Search,
Bayesian Optimization xây dựng một mô hình xấp xỉ cho hàm mục tiêu dựa trên các
lần đánh giá trước đó, sau đó lựa chọn những bộ siêu tham số có khả năng mang
lại hiệu quả cao để tiếp tục thử nghiệm. Do mỗi lần đánh giá tương ứng với việc
huấn luyện và kiểm định lại mô hình, phương pháp này giúp giảm số lần thử
nghiệm cần thiết trong khi vẫn có khả năng tìm kiếm được cấu hình tốt.

Giả sử $\lambda$ là một cấu hình siêu tham số cần tối ưu và $f$ là hàm mục tiêu
cần cực đại, bài toán có thể viết dưới dạng:

\[
  \lambda^{*}
  =
  \arg\max_{\lambda\in\Lambda} f(\lambda).
\]

Bayesian Optimization không chọn các điểm thử nghiệm một cách độc lập. Thay
vào đó, phương pháp này xây dựng một mô hình xác suất thay thế để xấp xỉ quan
hệ giữa siêu tham số và giá trị hàm
mục tiêu dựa trên các lần thử trước đó. Tại mỗi vòng lặp, một \textbf{hàm thu
nhận} được sử dụng để quyết định điểm thử tiếp theo, cân bằng giữa hai mục tiêu:

\begin{itemize}[leftmargin=*]
  \item \textbf{Khai thác}: thử các vùng đã có dấu hiệu cho kết
        quả tốt.
  \item \textbf{Khám phá}: thử các vùng còn nhiều bất định, có
        khả năng chứa nghiệm tốt hơn.
\end{itemize}

Trong Bayesian Optimization, hàm thu nhận \textit{Expected Improvement}, viết
tắt EI, đo mức cải thiện kỳ vọng của một cấu hình siêu tham số mới so với giá
trị tốt nhất hiện tại $f_{\text{best}}$:

\[
  \text{EI}(\lambda)
  =
  \mathbb{E}
  \left[
    \max(f(\lambda)-f_{\text{best}},0)
  \right].
\]

EI cân bằng giữa hai yếu tố: khai thác các vùng có khả năng cho kết quả tốt và
khám phá các vùng chưa được đánh giá đầy đủ. Nhờ tận dụng thông tin từ những
lần thử nghiệm trước, Bayesian Optimization có thể tìm kiếm cấu hình siêu tham
số hiệu quả với ít lần đánh giá hơn so với các phương pháp tìm kiếm trực tiếp.

Trong thực nghiệm, nghiên cứu sử dụng \textbf{Optuna} với bộ lấy mẫu
\textbf{TPE}, viết tắt của Tree-structured Parzen Estimator, một biến thể của
Bayesian Optimization. Thay vì mô hình hóa trực tiếp giá trị mục tiêu, TPE ước lượng
phân phối của các bộ siêu tham số dựa trên kết quả quan sát được, ưu tiên những
cấu hình có khả năng thuộc nhóm cho hiệu quả tốt. Cơ chế này giúp giảm số lần
huấn luyện cần thiết khi tìm kiếm trong không gian siêu tham số.

Do bài toán cảnh báo sớm có dữ liệu mất cân bằng giữa hai lớp, tiêu chí tối ưu
cần phản ánh khả năng phát hiện sinh viên có nguy cơ. Vì vậy, nghiên cứu sử
dụng F1-score của lớp At-risk làm hàm mục tiêu, nhằm cân bằng giữa precision và
recall thay vì chỉ tối ưu độ chính xác tổng thể.

\section{Diễn giải mô hình bằng SHAP}
\label{sec:shap_theory}

Trong bài toán cảnh báo sớm rủi ro học tập, khả năng diễn giải mô hình có ý
nghĩa đặc biệt quan trọng. Một hệ thống dự báo không chỉ cần xác định những
sinh viên có nguy cơ mà còn cần cung cấp thông tin về các yếu tố liên quan đến
kết quả dự báo, qua đó hỗ trợ giảng viên và cố vấn học tập lựa chọn phương án
can thiệp phù hợp.

\textbf{SHAP}, viết tắt của SHapley Additive exPlanations, là một phương pháp
diễn giải mô hình dựa trên lý thuyết giá trị Shapley trong lý thuyết trò chơi.
Ý tưởng chính của SHAP là phân bổ đóng góp của từng đặc trưng vào kết quả dự
báo, từ đó giải thích cách mô hình đưa ra quyết định đối với từng quan sát cụ
thể.

Với một mô hình dự báo $f$ và một quan sát $\mathbf{x}$, SHAP biểu diễn đầu ra
của mô hình dưới dạng một mô hình cộng tính:

\[
  f(\mathbf{x}) = \phi_0 + \sum_{j=1}^{p}\phi_j,
\]

trong đó $\phi_0$ là giá trị nền, thường tương ứng với giá trị kỳ vọng của mô
hình trên tập dữ liệu tham chiếu, còn $\phi_j$ biểu diễn mức đóng góp của đặc
trưng thứ $j$ đối với dự báo của mẫu đang xét. Nếu
$\phi_j > 0$, đặc trưng đó làm tăng giá trị đầu ra của mô hình; ngược lại,
$\phi_j < 0$ cho thấy đặc trưng đó làm giảm giá trị dự báo. Đối với bài toán
phân loại nhị phân, giá trị SHAP có thể được tính trên các không gian đầu ra
khác nhau của mô hình, phổ biến là log-odds hoặc xác suất dự báo.

Giá trị SHAP của một đặc trưng được xây dựng dựa trên giá trị Shapley, tức là
mức đóng góp biên trung bình của đặc trưng đó trên tất cả các tập hợp đặc trưng
có thể có:

\[
  \phi_j
  =
  \sum_{S \subseteq F \setminus \{j\}}
  \frac{|S|!(p-|S|-1)!}{p!}
  \left[
    f_{S\cup\{j\}}(\mathbf{x}_{S\cup\{j\}})
    -
    f_S(\mathbf{x}_S)
  \right],
\]

trong đó $F$ là tập hợp toàn bộ đặc trưng, $S$ là một tập con không chứa đặc
trưng $j$. Thành phần trong ngoặc vuông biểu diễn mức thay đổi của dự báo khi
bổ sung đặc trưng $j$ vào tập $S$. Hệ số

\[
\frac{|S|!(p-|S|-1)!}{p!}
\]

đảm bảo rằng mọi thứ tự kết hợp các đặc trưng đều được xem xét với trọng số
công bằng.

SHAP có ba tính chất quan trọng giúp nó trở thành một phương pháp diễn giải
phổ biến:

\begin{itemize}[leftmargin=*]
  \item \textbf{Tính cộng tính}: tổng các giá trị SHAP của tất cả đặc trưng
        cộng với giá trị nền bằng đúng đầu ra của mô hình.
  \item \textbf{Tính nhất quán}: nếu mức đóng góp biên của một đặc trưng tăng
        lên khi mô hình thay đổi, giá trị SHAP của đặc trưng đó cũng không
        giảm.
  \item \textbf{Tính cục bộ}: mỗi dự báo riêng lẻ có thể được phân rã thành
        đóng góp của từng đặc trưng, cho phép giải thích quyết định của mô hình
        đối với từng sinh viên.
\end{itemize}

Trong thực tế, việc tính trực tiếp giá trị Shapley theo công thức trên là không
khả thi khi số lượng đặc trưng lớn, do cần xét toàn bộ $2^p$ tập con đặc trưng.
Đối với các mô hình dựa trên cây quyết định, thuật toán
\textbf{TreeSHAP} khai thác cấu trúc của cây để tính toán chính xác giá trị SHAP
với chi phí thấp hơn đáng kể. Vì vậy, SHAP đặc biệt phù hợp để phân tích các mô
hình như Random Forest, CatBoost và LightGBM được sử dụng trong nghiên cứu này.

Kết quả SHAP có thể được phân tích ở hai mức độ. Ở mức độ toàn cục, giá trị
trung bình của $|\phi_j|$ trên nhiều mẫu cho biết mức độ quan trọng tương đối
của từng đặc trưng trong toàn bộ mô hình. Ở mức độ cục bộ, các biểu đồ như
waterfall plot hoặc force plot cho thấy cách từng đặc trưng tác động đến dự báo
của một sinh viên cụ thể, theo hướng làm tăng nguy cơ At-risk hoặc hướng về
nhóm Pass.

Nhờ khả năng kết hợp giữa đánh giá độ quan trọng đặc trưng và giải thích dự báo
ở cấp độ cá nhân, SHAP không chỉ giúp phân tích cách mô hình hoạt động mà còn
hỗ trợ kiểm tra liệu các quyết định của mô hình có phù hợp với kiến thức chuyên
môn trong lĩnh vực giáo dục hay không.
% =====================================================================
%  CHƯƠNG 2 — PHÂN TÍCH KHÁM PHÁ DỮ LIỆU
% =====================================================================
\chapter{Phân tích khám phá dữ liệu}
\label{part:eda}

Sau khi trình bày cơ sở lý thuyết của các thuật toán và phương pháp hỗ trợ ở
Chương~\ref{part:theory}, phần này phân tích khám phá bộ dữ liệu OULAD nhằm rút
ra các định hướng tiền xử lý, thiết kế đặc trưng và đánh giá phù hợp cho bước
xây dựng, tinh chỉnh và diễn giải mô hình ở Chương~\ref{part:model}.

\section{Tổng quan bộ dữ liệu OULAD}

Bộ dữ liệu OULAD được tổ chức thành bảy bảng dữ liệu liên kết với nhau qua các
định danh duy nhất, bao gồm các nhóm thông tin: bài đánh giá, học phần, thông tin
sinh viên, đăng ký học, kết quả bài đánh giá và nhật ký tương tác của sinh viên
với hệ thống học trực tuyến VLE. Sơ đồ quan hệ giữa các bảng được minh hoạ ở
Hình~\ref{fig:erd}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.98\textwidth]{oulad_erd.jpg}
  \caption{Sơ đồ quan hệ thực thể ERD của bộ dữ liệu OULAD.}
  \label{fig:erd}
\end{figure}

Quy mô tổng thể của bộ dữ liệu được tóm tắt trong Bảng~\ref{tab:overview}.

\begin{table}[H]
  \centering
  \caption{Thống kê tổng quan bộ dữ liệu OULAD.}
  \label{tab:overview}
  \begin{tabular}{llr}
    \toprule
    \textbf{Bảng gốc} & \textbf{Thực thể} & \textbf{Số lượng} \\
    \midrule
    \code{studentInfo}         & Sinh viên tham gia khóa học & 32{,}953 \\
    \code{courses}             & Đợt mở học phần             & 22 \\
    \code{vle}                 & Trang/tài nguyên VLE        & 6{,}364 \\
    \code{studentVle}          & Nhật ký tương tác VLE       & 10{,}655{,}280 \\
    \code{studentRegistration} & Bản ghi đăng ký học         & 32{,}953 \\
    \code{assessments}         & Bài đánh giá                & 206 \\
    \code{studentAssessment}   & Bản ghi kết quả đánh giá    & 173{,}912 \\
    ---                        & \textbf{Tổng số thuộc tính} & \textbf{43} \\
    \bottomrule
  \end{tabular}
\end{table}

Mỗi đợt mở học phần được định danh bằng mã \code{code\_presentation} gồm
\textbf{năm khai giảng} kèm \textbf{một chữ cái chỉ tháng khai giảng}. Chữ cái
này mã hoá tháng theo \textbf{vị trí của nó trong bảng chữ cái}: A là chữ thứ 1
ứng với tháng 1, B là chữ thứ 2 ứng với tháng 2, \ldots, J là chữ thứ 10 ứng với
tháng 10. Trong bộ dữ liệu chỉ xuất hiện hai giá trị: \code{B} tương ứng
\textbf{tháng 2} và \code{J} tương ứng \textbf{tháng 10}. Ví dụ, mã
\code{2013J} là đợt khai giảng tháng 10 năm 2013, còn \code{2014B} là đợt khai
giảng tháng 2 năm 2014. Mọi mốc thời gian trong dữ liệu như ngày nộp bài, ngày
đăng ký và ngày tương tác VLE đều được tính tương đối so với ngày bắt đầu khoá
học, tức ngày 0, cho phép so sánh nhất quán giữa các đợt học khác nhau.

\section{Phân tích dữ liệu đánh giá}

\subsection{Cấu trúc chung}

Dữ liệu đánh giá được ghép với thông tin học phần để bổ sung độ dài của từng
đợt học. Sau khi làm sạch, dữ liệu có các đặc điểm sau:

\begin{itemize}[leftmargin=*]
  \item Bộ dữ liệu gồm \textbf{7 học phần}: AAA, BBB, CCC, DDD, EEE, FFF và GGG.
        Trong đó FFF có nhiều bài đánh giá nhất với 52 bài.
  \item Có \textbf{4 đợt mở học phần}: 2013B, 2013J, 2014B và 2014J.
  \item Có \textbf{3 loại bài đánh giá}, trong đó TMA chiếm nhiều nhất với
        106/206 bài, tương đương $\approx 51\%$:
    \begin{itemize}
      \item \textbf{TMA} --- Tutor Marked Assessment, bài đánh giá do
            giảng viên hoặc trợ giảng chấm thủ công, thường là bài tập tự luận
            nộp theo từng mốc trong khoá học.
      \item \textbf{CMA} --- Computer Marked Assessment, bài đánh giá
            được hệ thống chấm tự động bằng máy tính, thường ở dạng trắc nghiệm.
      \item \textbf{Exam} --- bài thi tổng kết, quyết
            định phần lớn hoặc toàn bộ kết quả học phần.
    \end{itemize}
  \item Toàn bộ 206 bài đánh giá đều có mã định danh duy nhất ---
        \textbf{không có bài đánh giá nào bị trùng lặp}.
\end{itemize}

Bảng~\ref{tab:desc_assessment} tóm tắt thống kê mô tả các biến phân loại của dữ
liệu đánh giá.

\input{table_desc_assessment}

Phân phối số bài đánh giá theo học phần và tỷ lệ các loại bài đánh giá lần lượt
được thể hiện ở Hình~\ref{fig:module_dist} và Hình~\ref{fig:assess_type}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{module_dist.png}
  \caption{Phân phối số bài đánh giá theo mã học phần.}
  \label{fig:module_dist}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{assessment_type_pie.png}
  \caption{Tỷ lệ các loại bài đánh giá: TMA, CMA và Exam.}
  \label{fig:assess_type}
\end{figure}

\subsection{Phân tích trọng số và phát hiện bất thường}

Tổng trọng số \code{weight} của các bài đánh giá được tính theo từng cặp học
phần--đợt học, qua đó ghi nhận một số điểm đáng chú ý:

\begin{itemize}[leftmargin=*]
  \item Phần lớn học phần gồm AAA, BBB, DDD, EEE và FFF có tổng trọng số $= 200$
        theo quy ước chung: 100\% cho nhóm bài TMA/CMA và 100\% cho bài thi
        cuối kỳ. Ngoài ra, không phải học phần nào cũng được mở đủ cả 4 đợt ---
        ví dụ AAA chỉ có 2013J và 2014J.
  \item \textbf{GGG có tổng trọng số $= 100$:} tất cả bài TMA và CMA của GGG đều
        có \code{weight} $= 0$, chỉ bài Exam mới có \code{weight} $= 100$. Nói
        cách khác, kết quả học phần GGG phụ thuộc \textbf{hoàn toàn} vào bài thi
        cuối kỳ.
  \item \textbf{CCC có tổng trọng số $= 300$:} học phần CCC có 2 bài Exam trong
        mỗi đợt, mỗi bài có \code{weight} $= 100$; cộng với TMA + CMA $= 100$, dẫn
        đến tổng bằng 300.
  \item Ở học phần CCC, giá trị ngày thi bị thiếu, ký hiệu \code{'?'}, ở các bài
        thuộc loại Exam.
\end{itemize}

\subsection{Giá trị thiếu ở biến ngày thi}

Kiểm tra các bài có giá trị ngày thi bị thiếu cho thấy:

\begin{itemize}[leftmargin=*]
  \item Giá trị \code{'?'} chỉ xuất hiện ở loại Exam, không có ở TMA hay CMA ---
        đây là dạng \textbf{thiếu có tính hệ thống}, không ngẫu nhiên.
  \item Có 11/24 bản ghi Exam bị thiếu ngày, chiếm gần một nửa, khoảng
        $\approx 45.8\%$.
  \item Mức độ thiếu theo học phần:
    \begin{itemize}
      \item AAA, BBB, CCC: thiếu hoàn toàn --- không đợt học nào có ngày thi
            chính xác.
      \item DDD: chỉ thiếu 1 đợt (2014J), 3 đợt còn lại đầy đủ.
      \item EEE, FFF, GGG: đầy đủ ngày thi cho tất cả các đợt.
    \end{itemize}
  \item Nhiều khả năng các học phần này có lịch thi linh hoạt nên ngày thi
        không được ấn định sẵn trong hệ thống. Do bài thi cuối kỳ thường nằm
        ngoài khoảng thời gian dự báo, ảnh hưởng thực tế của việc thiếu này là
        \textbf{không đáng kể}.
\end{itemize}

\section{Phân tích hành vi tương tác VLE}

Dữ liệu tương tác VLE được tổng hợp về mức \textbf{tuần}: với mỗi sinh viên, số
lần click được cộng dồn theo tuần, trong đó tuần được tính bằng
$\lfloor \text{ngày}/7 \rfloor$ kể từ ngày bắt đầu học phần. Sau khi tổng hợp,
dữ liệu tương tác theo tuần còn \textbf{627{,}031} bản ghi --- giảm khoảng 4 lần
so với 2{,}518{,}170 dòng của dữ liệu chi tiết ban đầu; mỗi bản ghi là tổng số
click của một sinh viên trong một tuần.

\subsection{Đặc điểm phân phối}

Bảng~\ref{tab:desc_vle} trình bày thống kê mô tả của số tuần \code{week} và số
click theo tuần \code{weekly\_clicks}.

\input{table_desc_vle}

\begin{itemize}[leftmargin=*]
  \item \textbf{Biến \code{week}:} giá trị nhỏ nhất $= -4$ cho thấy một số sinh
        viên đã truy cập VLE \textit{trước} khi khoá học chính thức bắt đầu. 50\%
        số bản ghi nằm trong khoảng tuần 4--23, tương ứng giai đoạn giữa khoá.
        Giá trị lớn
        nhất $= 38$ ($\approx 9.5$ tháng) phù hợp với độ dài các học phần trong
        OULAD ($\approx 240$--$270$ ngày).
  \item \textbf{Biến \code{weekly\_clicks}:} phân phối lệch phải nặng ---
        trung vị chỉ bằng 30 trong khi trung bình lên tới 63.16, chứng tỏ giá
        trị trung bình bị đẩy lên bởi các giá trị ngoại lai; cá biệt có giá trị
        lớn nhất bằng $6{,}999$ trong khi $Q_3 = 78$. Độ lệch chuẩn 96.59 còn
        lớn hơn cả giá trị trung bình, cho thấy mức độ phân tán rất lớn.
\end{itemize}

Để mô tả rõ hơn đuôi phải của phân phối, Hình~\ref{fig:pctl} biểu diễn giá trị
\code{weekly\_clicks} theo các phân vị.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{weekly_clicks_percentile.png}
  \caption{Giá trị \code{weekly\_clicks} theo các phân vị.}
  \label{fig:pctl}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item Từ P0 đến P75, \code{weekly\_clicks} tăng chậm từ 1 đến khoảng 80 ---
        phần lớn sinh viên tương tác ở mức vừa phải mỗi tuần.
  \item Từ P75 trở đi, đường cong dốc lên nhanh --- dấu hiệu của phân phối lệch
        phải với đuôi dài.
  \item $P_{90} = 159$ và $P_{95} = 232$: nhóm 10\% sinh viên tích cực nhất có
        lượng click gấp $\approx 2.5$ lần mức trung bình chung 63.16.
\end{itemize}

\section{Xây dựng tập dữ liệu dạng snapshot}

\subsection{Thiết kế panel theo thời gian}
\label{sec:panel_design}

Để phù hợp với mục tiêu \textit{dự báo sớm}, dữ liệu được tái cấu trúc thành
dạng panel theo thời gian. Với mỗi mốc thời gian
$T \in \{30, 60, 90, 120, 150, 180, 210, 240\}$, tính theo ngày kể từ đầu khoá,
ta tạo
một \textit{snapshot} thể hiện trạng thái của từng sinh viên tại thời điểm đó.
Mỗi sinh viên xuất hiện tối đa 8 lần, tương ứng với 8 snapshot.

Tại mỗi mốc $T$, quy trình tính đặc trưng như sau:

\begin{itemize}[leftmargin=*]
  \item \textbf{Lọc sinh viên còn hoạt động:} loại bỏ những sinh viên đã hủy
        đăng ký trước ngày $T$.
  \item \textbf{Đặc trưng VLE}: chỉ lấy dữ liệu đến tuần tương ứng với $T$,
        gồm \code{total\_clicks}, \code{active\_weeks} và
        \code{avg\_weekly\_clicks}.
  \item \textbf{Đặc trưng đánh giá}: chỉ lấy bài có hạn nộp $\le T$ và đã nộp
        trước $T$, gồm \code{num\_submitted}, \code{avg\_score},
        \code{avg\_days\_early} và \code{num\_failed}.
  \item \textbf{Đặc trưng tĩnh và nhãn:} thông tin nhân khẩu học của sinh viên
        và nhãn \code{final\_result}.
\end{itemize}

\textbf{Lưu ý:} học phần GGG được loại khỏi tập dữ liệu do đặc thù trọng số bất
thường đã phân tích ở trên --- kết quả của học phần này phụ thuộc hoàn toàn vào
bài thi cuối kỳ nên không phù hợp với cách dự báo dựa trên hành vi tích luỹ.

\subsection{Quy mô và giá trị thiếu}

Tập dữ liệu thu được gồm \textbf{178{,}294 dòng} và \textbf{22 biến}, bao gồm
thông tin nhân khẩu học, hành vi học tập VLE, kết quả bài tập và kết quả cuối
kỳ, với nhiều kiểu dữ liệu: 10 biến dạng phân loại, 10 biến số thực và 2 biến số
nguyên.

Bảng~\ref{tab:snapshot_features} tóm tắt ý nghĩa của các biến gốc xuất hiện
trong tập snapshot. Các biến định danh, mốc dự báo và nhãn được giữ trong bảng
để làm rõ cấu trúc dữ liệu, nhưng không được xem là đặc trưng huấn luyện.

\input{table_snapshot_features}

Bảng~\ref{tab:null} liệt kê các biến có giá trị thiếu cùng tỷ lệ tương ứng.

\input{table_null}

Nhận xét về giá trị thiếu:

\begin{itemize}[leftmargin=*]
  \item Biến \textbf{ngày hủy đăng ký} thiếu nhiều nhất, khoảng $\approx 91.0\%$, vì
        phần lớn sinh viên theo học đến hết khoá nên không có ngày hủy đăng ký
        --- đây là dạng thiếu mang ý nghĩa tích cực.
  \item Các biến VLE gồm \code{total\_clicks}, \code{active\_weeks} và
        \code{avg\_weekly\_clicks}, thiếu $\approx 1.7\%$, nhiều khả năng do một
        số sinh viên chưa từng truy cập hệ thống.
  \item Các biến đánh giá như \code{avg\_score} và \code{num\_submitted} thiếu
        đáng kể ($\approx 9$--$10\%$), do một số sinh viên chưa nộp bài tại thời
        điểm dự báo.
\end{itemize}

Ngoài dạng thiếu thông thường là giá trị rỗng, biến \code{imd\_band} còn có
một dạng thiếu \textbf{được mã hoá bằng ký hiệu \code{'?'}}, chiếm khoảng $4\%$
số sinh viên. Dạng thiếu này không xuất hiện khi thống kê giá trị rỗng nên cần
được nhận diện và xử lý riêng; phân bố cụ thể sẽ được phân tích ở
Mục~\ref{sec:imd}.

\subsection{Phân phối nhãn mục tiêu}

Phân phối nhãn \code{final\_result} ban đầu gồm bốn lớp, như
Hình~\ref{fig:result4}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{final_result_4.png}
  \caption{Phân phối nhãn \code{final\_result} gồm bốn lớp.}
  \label{fig:result4}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item \textbf{Pass} chiếm tỷ lệ cao nhất với 89{,}944 bản ghi, tương ứng
        50.4\%.
  \item \textbf{Fail} đứng thứ hai với 50{,}525 bản ghi, tương ứng 28.3\% ---
        gần 1/3
        lượt đăng ký không đạt, là nhóm quan trọng nhất cần được dự báo sớm.
  \item \textbf{Distinction} chiếm 11.8\%, tương ứng 21{,}024 bản ghi --- nhóm sinh viên
        xuất sắc.
  \item \textbf{Withdrawn} chiếm 9.4\%, tương ứng 16{,}801 bản ghi --- lượt đăng ký bị
        hủy giữa chừng; nhóm này thường bộc lộ dấu hiệu sớm qua mức tương tác
        thấp.
\end{itemize}

Mỗi bản ghi tương ứng với một sinh viên, một học phần và một đợt học tại
một snapshot $T$;
một sinh viên có thể xuất hiện nhiều lần nếu đăng ký nhiều học phần hoặc nhiều
đợt. Với bài toán cảnh báo sớm, việc phân biệt Distinction với Pass là không cần
thiết --- mục tiêu là phát hiện sinh viên có nguy cơ trượt hoặc bỏ học. Do đó,
\textbf{Distinction được gộp vào Pass}; phân phối mới được thể hiện ở
Hình~\ref{fig:result3}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{final_result_3.png}
  \caption{Phân phối nhãn sau khi gộp Distinction vào Pass, còn ba lớp.}
  \label{fig:result3}
\end{figure}

Sau khi gộp, tỷ lệ Pass : Fail : Withdrawn $= 62.2\% : 28.3\% : 9.4\%$, cho thấy
tập dữ liệu có mức \textbf{mất cân bằng nhãn vừa phải} --- cần lưu ý ở bước xây
dựng mô hình.

\subsection{Chia tập huấn luyện và kiểm tra}

Tập dữ liệu được chia thành tập train và test theo
\textbf{từng sinh viên}, đảm bảo toàn bộ lịch sử của một sinh viên chỉ nằm trong
một tập duy nhất. Nếu chia theo từng dòng, cùng một sinh viên có thể xuất hiện ở cả train
lẫn test với các snapshot khác nhau. Khi đó, mô hình được huấn luyện trên chính
hành vi của sinh viên có mặt trong tập kiểm tra, dẫn đến hiện tượng rò rỉ dữ
liệu và làm sai lệch kết quả đánh giá.

Với tỷ lệ train:test $= 80:20$, kết quả chia được tóm tắt ở
Bảng~\ref{tab:split}.

\begin{table}[H]
  \centering
  \caption{Kết quả chia tập train/test theo sinh viên.}
  \label{tab:split}
  \begin{tabular}{lrr}
    \toprule
    \textbf{Tập} & \textbf{Số sinh viên} & \textbf{Số snapshot} \\
    \midrule
    Train & 17{,}921 & 142{,}384 \\
    Test  & 4{,}481  & 35{,}910 \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Phân tích khám phá các đặc trưng tĩnh}

Thông tin tĩnh của sinh viên được xem xét theo hai nhóm phục vụ phân tích:

\begin{itemize}[leftmargin=*]
  \item \textbf{Thông tin nhân khẩu học}: \code{gender}, \code{region},
        \code{age\_band}, \code{imd\_band}, \code{highest\_education},
        \code{disability}; xét mỗi sinh viên một lần sau khi loại bỏ trùng lặp.
  \item \textbf{Đặc trưng hành vi tĩnh}: \code{num\_of\_prev\_attempts} và
        \code{studied\_credits}, gắn với từng lượt đăng ký học phần, kèm nhãn kết
        quả \code{final\_result}.
\end{itemize}

\subsection{Chỉ số thiếu thốn kinh tế \code{imd\_band}}
\label{sec:imd}

Biến \code{imd\_band} đo mức độ thiếu thốn kinh tế theo khu vực địa lý, được kỳ
vọng có liên quan đến kết quả học tập.
Hình~\ref{fig:imd_dist} thể hiện phân phối của biến này.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{imd_dist.png}
  \caption{Phân phối \code{imd\_band} trong tập huấn luyện; nhóm \code{'?'}
  được tô màu cam.}
  \label{fig:imd_dist}
\end{figure}

Phân phối \code{imd\_band} tập trung cao nhất ở band 30--40\% (1{,}925 sinh
viên) và giảm dần về hai phía. Nhìn chung, phần lớn sinh viên đến từ các khu
vực có mức độ thiếu thốn trung bình đến cao, trong khi số sinh viên ở các khu
vực khá giả ít hơn. Đáng chú ý, có 720 sinh viên, tương ứng $\approx 4.2\%$, mang giá trị
\code{'?'}, cần được xử lý trước khi đưa vào mô hình.

\paragraph{Giá trị thiếu theo vùng.}
Giá trị \code{'?'} trong \code{imd\_band} tập trung gần như hoàn toàn ở hai
vùng: North Region chiếm 64.86\% và Ireland chiếm 25.42\% --- cộng lại chiếm
$\approx 90.3\%$ tổng số giá trị thiếu; các vùng còn lại chỉ đóng góp
$\approx 9.7\%$.
Xét theo chiều ngược lại, tức tỷ lệ \code{'?'} trong mỗi vùng:

\begin{itemize}[leftmargin=*]
  \item North Region đạt 45.65\% và Ireland đạt 23.40\% về tỷ lệ \code{'?'}; hai vùng này vượt
        trội so với tất cả các vùng còn lại (đều $\le 2\%$), cho thấy tình trạng
        thiếu dữ liệu này gắn với đặc thù của từng vùng địa lý.
  \item North Western, Yorkshire và West Midlands là những vùng tập trung nhiều
        sinh viên thuộc band IMD thấp nhất 0--10\%, với tỷ lệ lần lượt 23.09\%,
        19.42\% và 17.52\%. London cũng nghiêng về các mức IMD thấp:
        10--20\% đạt 19.30\% và 20--30\% đạt 18.37\%.
  \item South Region tập trung ở các band cao, với 90--100\% đạt 21.74\% và
        80--90\% đạt 15.55\%; South East Region
        tập trung ở các band cao. East Anglian Region có xu hướng tương tự.
  \item Scotland, East Midlands, Wales và South West Region không lệch rõ về phía
        nào, các band dao động tương đối đều trong khoảng 7--13\%.
\end{itemize}

Bảng~\ref{tab:imd_region} trình bày đầy đủ phân phối \code{imd\_band} theo từng
vùng, chuẩn hoá theo hàng để làm rõ các nhận xét trên.

\input{table_imd_region}

\paragraph{Giá trị thiếu theo trình độ học vấn.}
Xét quan hệ giữa \code{imd\_band} và \code{highest\_education}:

\begin{itemize}[leftmargin=*]
  \item \textit{No Formal quals} tập trung mạnh ở các band thấp (20--30\%:
        22.50\%; 0--10\%: 18.75\%\ldots), gần như không xuất hiện ở band 80--90\%.
        Tỷ lệ \code{'?'} đạt 9.38\%, cao thứ hai.
  \item \textit{Lower Than A Level} giảm dần từ band thấp đến band cao, từ
        11.43\% xuống còn 6.39\%. Tỷ lệ \code{'?'} đạt 3.72\%.
  \item \textit{A Level or Equivalent} phân phối tương đối đồng đều, trong khoảng
        8--11\%. Tỷ lệ \code{'?'} đạt 2.34\%.
  \item \textit{HE Qualification} phân bố đồng đều chủ yếu ở các band trung bình
        đến cao, nhóm 90--100\% chiếm tỷ lệ lớn nhất, đạt 10.95\%. Tỷ lệ
        \code{'?'} đạt 6.68\%.
  \item \textit{Post Graduate Qualification} có tỷ lệ \code{'?'} cao nhất,
        đạt 41.85\%; gần như không có sinh viên ở band 0--10\% và tập trung rõ
        ở band 90--100\%, đạt 25\%.
\end{itemize}

Bảng~\ref{tab:imd_edu} trình bày đầy đủ các con số này.

\input{table_imd_edu}

Hình~\ref{fig:edu_imd_extreme} và Hình~\ref{fig:edu_imd_middle} trực quan hoá xu
hướng này. Ở nhóm band cực trị trong Hình~\ref{fig:edu_imd_extreme}, hai band
thấp 0--10\%, 10--20\% và hai band cao 80--90\%, 90--100\% biến động ngược chiều
nhau theo trình độ học vấn: tỷ trọng band thấp giảm mạnh còn band cao tăng dần
khi học vấn tăng --- đặc biệt band 90--100\% tăng vọt ở nhóm Post Graduate
Qualification. Bốn đường gần như giao nhau quanh mức \textit{A Level or
Equivalent}, cho thấy đây là ngưỡng học vấn mà mối liên hệ với \code{imd\_band}
bắt đầu đảo chiều. Ở nhóm band trung gian 20--80\% trong
Hình~\ref{fig:edu_imd_middle}, các đường biến động khá tương đồng và không
band nào nổi bật hẳn.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.93\textwidth]{edu_imd_extreme.png}
  \caption{Tỷ lệ \code{imd\_band} theo trình độ học vấn, gồm nhóm band cực trị
  0--20\% và 80--100\%.}
  \label{fig:edu_imd_extreme}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.93\textwidth]{edu_imd_middle.png}
  \caption{Tỷ lệ \code{imd\_band} theo trình độ học vấn, gồm nhóm band trung gian
  20--80\%.}
  \label{fig:edu_imd_middle}
\end{figure}

\subsection{Tình trạng khuyết tật và lịch sử học lại}

Phân phối \code{disability} trong tập huấn luyện được thể hiện ở
Hình~\ref{fig:disability}: phần lớn sinh viên không khai báo khuyết tật
chiếm 91.1\%, còn nhóm có khuyết tật chỉ chiếm 8.9\%.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.73\textwidth]{disability_dist.png}
  \caption{Phân phối tình trạng khuyết tật.}
  \label{fig:disability}
\end{figure}

Bên cạnh khuyết tật, biến \code{num\_of\_prev\_attempts} cho biết số lần sinh
viên đã đăng ký học phần này trước đây, qua đó phân biệt sinh viên học lần đầu
với sinh viên học lại. Phân phối được thể hiện ở Hình~\ref{fig:prev}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{prev_attempts.png}
  \caption{Phân phối số lần đăng ký học phần trước đó.}
  \label{fig:prev}
\end{figure}

Từ biến này, ta tạo biến nhị phân \code{is\_retake\_the\_course}: nhận giá trị
1 nếu sinh viên đã từng đăng ký học phần trước đó và 0 nếu học lần đầu. Các mức
học lại 1, 2, 3\ldots được gộp thành một nhóm duy nhất, vì điều quan trọng là
sinh viên \textit{đã từng} phải học lại hay chưa, chứ không phải học lại bao
nhiêu lần. Phân phối của biến mới được trình bày ở Hình~\ref{fig:retake}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\textwidth]{is_retake.png}
  \caption{Phân phối biến \code{is\_retake\_the\_course}.}
  \label{fig:retake}
\end{figure}

Phần lớn sinh viên, chiếm 86.5\%, đăng ký học phần lần đầu; 13.5\% còn lại là các
trường hợp học lại. Tỷ lệ này giảm rất nhanh theo số lần học lại: học lại lần
đầu chiếm 10.8\%, lần hai chỉ còn 2.1\%, và từ ba lần trở lên rất hiếm
với tỷ lệ dưới 0.5\%. Điều này cho thấy phần lớn sinh viên không đăng ký học lại sau khi
trượt học phần.

\paragraph{Học lại và tình trạng khuyết tật.}
Hình~\ref{fig:retake_dis} cho thấy nhóm sinh viên khuyết tật có tỷ lệ học lại
20.1\%, cao hơn rõ rệt so với nhóm không khuyết tật 12.9\%. Kết quả này gợi
ý khuyết tật là một yếu tố rủi ro trong học tập: nhóm này gặp nhiều trở ngại
hơn nên phải học lại thường xuyên hơn.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.75\textwidth]{retake_disability.png}
  \caption{Tỷ lệ học lại theo tình trạng khuyết tật.}
  \label{fig:retake_dis}
\end{figure}

\subsection{Phân tích theo nhãn mục tiêu}

\subsubsection{Đặc điểm ba nhóm kết quả}

Giá trị trung bình của các đặc trưng tĩnh theo từng nhóm kết quả được chuẩn hoá
và biểu diễn bằng biểu đồ radar ở Hình~\ref{fig:radar_static}. Từ biểu đồ, có
thể nhận thấy ba nhóm mang ba đặc điểm rủi ro khác biệt:

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{radar_static.png}
  \caption{Biểu đồ radar các đặc trưng tĩnh theo nhóm kết quả.}
  \label{fig:radar_static}
\end{figure}

Giá trị trung bình của từng đặc trưng theo nhóm kết quả --- số liệu gốc của
biểu đồ radar --- được trình bày ở Bảng~\ref{tab:mean_static}.

\input{table_mean_static}

\begin{itemize}[leftmargin=*]
  \item \textbf{Pass} là nhóm an toàn nhất: đa giác của nhóm này nhỏ nhất, tức
        các chỉ số rủi ro đều ở mức thấp. Phần lớn sinh viên học lần đầu với
        khối lượng đăng ký vừa phải, tỷ lệ khuyết tật thấp nhất; tỷ lệ sinh
        viên trẻ cũng thấp nhất, cho thấy nhóm lớn tuổi có xu hướng hoàn thành
        học phần tốt hơn.
  \item \textbf{Fail} nổi bật ở các đặc trưng về lịch sử học tập: số lần học
        trước và tỷ lệ học lại đều cao nhất, tỷ lệ sinh viên trẻ cũng cao nhất.
        Trong khi đó, khối lượng tín chỉ không cao hơn Pass đáng kể --- vấn đề
        của nhóm này nằm ở việc học lại nhiều lần mà kết quả chưa cải thiện,
        chứ không phải do học quá tải.
  \item \textbf{Withdrawn} có xu hướng ngược lại: khối lượng tín chỉ và tỷ lệ
        khuyết tật cao nhất, trong khi các chỉ số học lại chỉ ở mức trung bình.
        Nguyên nhân bỏ học vì vậy thiên về quá tải học tập và các trở ngại cá
        nhân hoặc sức khoẻ.
\end{itemize}

Như vậy, Fail và Withdrawn đại diện cho hai dạng rủi ro khác nhau: Fail gắn với
lịch sử học lại và nhóm sinh viên trẻ, còn Withdrawn gắn với khối lượng đăng ký
và tình trạng khuyết tật.

\subsubsection{Kết quả theo trình độ học vấn}

Hình~\ref{fig:edu_result} thể hiện tỷ lệ Pass / Fail / Withdrawn theo từng bậc
học vấn khi nhập học.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.93\textwidth]{edu_result.png}
  \caption{Tỷ lệ kết quả học tập theo trình độ học vấn.}
  \label{fig:edu_result}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item \textbf{Pass} tăng liên tục theo trình độ học vấn: từ nhóm \textit{No
        Formal quals} với 38.3\% lên nhóm \textit{Post Graduate Qualification}
        với 73.1\%.
  \item \textbf{Fail} giảm theo chiều ngược lại: từ 34.9\% xuống còn 11.5\% ---
        sinh viên có nền tảng học vấn cao hơn ít trượt môn hơn.
  \item \textbf{Withdrawn} cũng giảm nhưng chậm hơn Fail.
\end{itemize}

Đáng chú ý, \textit{No Formal quals} là nhóm rủi ro cao nhất: tỷ lệ Pass chỉ
đạt 38.3\%, trong khi Fail và Withdrawn cộng lại chiếm tới \textbf{61.7\%} ---
đây là nhóm duy nhất có tỷ lệ không hoàn thành vượt quá tỷ lệ hoàn thành.
\textit{Post Graduate Qualification} là nhóm duy nhất có tỷ lệ Withdrawn
15.4\%, cao hơn Fail 11.5\%: sinh viên sau đại học ít trượt môn nhưng lại có
xu hướng rút lui nhiều hơn, có thể vì lý do cá nhân hoặc công việc. Khoảng cách
giữa Pass và hai nhóm còn lại mở rộng rõ rệt từ \textit{A Level or Equivalent}
trở lên, cho thấy đây có thể là ngưỡng mà nền tảng học vấn bắt đầu tạo ra lợi
thế rõ ràng.

\subsubsection{Kết quả theo chỉ số thiếu thốn kinh tế}

Hình~\ref{fig:imd_result} thể hiện tỷ lệ Pass / Fail / Withdrawn theo từng nhóm
IMD.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.98\textwidth]{imd_result.png}
  \caption{Tỷ lệ kết quả học tập theo nhóm \code{imd\_band}.}
  \label{fig:imd_result}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item \textbf{Pass} tăng đều từ 43.4\% ở band 0--10\% lên 65.3\% ở band
        90--100\% ---
        sinh viên từ khu vực khá giả hơn có tỷ lệ hoàn thành cao hơn.
  \item \textbf{Fail} giảm từ 33.8\% xuống 18.5\% --- nghịch biến với mức độ
        kinh tế, giảm tương đối đều.
  \item \textbf{Withdrawn} giảm nhẹ và không đều, dao động 18--23\% ở các band
        giữa.
\end{itemize}

Đáng chú ý, mối liên hệ giữa tỷ lệ Pass và IMD \textbf{yếu hơn} rõ rệt so với
mối liên hệ giữa Pass và trình độ học vấn ở mục trước: tỷ lệ Pass tăng theo học
vấn với biên độ lớn hơn, từ 38.3\% lên 73.1\%, so với theo IMD, từ 43.4\% lên
65.3\%. Điều này gợi ý \textbf{nền tảng học vấn ảnh hưởng đến kết
quả học tập nhiều hơn điều kiện kinh tế}. Ngoài ra, tỷ lệ Withdrawn gần như
không thay đổi theo IMD, và nhóm \code{'?'} có tỷ lệ Pass cao bất thường,
đạt 71.6\%, cao nhất trong tất cả các nhóm.

\section{Phân tích khám phá các đặc trưng động}

Các đặc trưng động được tính trong khoảng thời gian $[0, T]$ tại mỗi mốc dự đoán, phản ánh
mức độ tương tác VLE và kết quả bài kiểm tra tích luỹ đến thời điểm đó. Bảy đặc
trưng động gồm: \code{total\_clicks}, \code{active\_weeks},
\code{avg\_weekly\_clicks}, \code{num\_submitted}, \code{avg\_score},
\code{num\_failed} và \code{avg\_days\_early}.

\subsection{Thống kê mô tả}

Thống kê mô tả của bảy đặc trưng động được trình bày ở
Bảng~\ref{tab:desc_dynamic}.

\input{table_desc_dynamic}

\begin{itemize}[leftmargin=*]
  \item Các đặc trưng VLE lệch phải mạnh: \code{total\_clicks} có độ lệch
        chuẩn 1{,}358 lớn hơn cả giá trị trung bình 1{,}072, giá trị
        lớn nhất lên tới $23{,}115$; \code{avg\_weekly\_clicks} cũng tương tự
        khi trung bình là 60.7 nhưng giá trị lớn nhất đạt 1{,}070.5.
  \item \code{num\_failed} có giá trị nhỏ nhất, trung vị và $Q_3$ đều bằng 0
        --- phần lớn sinh viên không trượt bài nào trong khoảng thời gian quan
        sát. Trung bình chỉ đạt $0.16$ nhưng giá trị lớn nhất lên tới 8, tức
        vẫn có một nhóm nhỏ trượt nhiều bài.
  \item \code{avg\_days\_early} trải trên khoảng giá trị rất rộng: từ $-123$,
        tức nộp trễ 123 ngày, đến $236$, tức nộp sớm 236 ngày. Trung vị bằng 0, với
        $Q_1 = -1.5$ và $Q_3 = 1.5$: khoảng một nửa số sinh viên nộp bài trong
        vòng $\pm 1.5$ ngày quanh hạn nộp.
  \item \code{avg\_score} tương đối tập trung: $Q_1 = 64.7$ và $Q_3 = 85$ ---
        phần lớn sinh viên đạt điểm trung bình ở mức khá trở lên.
\end{itemize}

\subsection{Số sinh viên còn học theo thời gian}

Hình~\ref{fig:retention} thể hiện số sinh viên còn hoạt động tại mỗi mốc dự đoán.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{retention.png}
  \caption{Số sinh viên còn hoạt động theo các mốc dự đoán cách nhau 30 ngày.}
  \label{fig:retention}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item Số sinh viên còn hoạt động giảm đều qua các mốc: từ \textbf{17{,}921}
        ở ngày 30 xuống \textbf{15{,}171} ở ngày 240 --- tổng cộng
        \textbf{2{,}750 sinh viên}, tương đương $\approx 15.3\%$, rút khỏi khoá học trong
        210 ngày quan sát.
  \item Mức giảm lớn nhất rơi vào giai đoạn đầu, từ ngày 30 sang ngày 60:
        $-629$; sau đó chậm dần và ổn định hơn về cuối khoá, từ ngày 180 sang
        ngày 210: $-214$; từ ngày 210 sang ngày 240: $-208$ --- chỉ bằng
        khoảng 1/3 giai đoạn đầu).
  \item Trung bình mỗi 30 ngày có $\approx 393$ sinh viên rời khoá học; tuy
        nhiên con số trung bình này chưa phản ánh đúng thực tế, vì phần lớn
        trường hợp rút lui tập trung ở giai đoạn đầu khoá.
\end{itemize}

\subsection{Phân tích tương quan}

Ma trận tương quan giữa các đặc trưng động được thể hiện ở
Hình~\ref{fig:corr}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{corr_matrix.png}
  \caption{Ma trận tương quan giữa các đặc trưng động.}
  \label{fig:corr}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item \textbf{Cụm hoạt động trực tuyến} (\code{total\_clicks},
        \code{active\_weeks}, \code{avg\_weekly\_clicks}) tương quan chặt với
        nhau; đặc biệt \code{total\_clicks} và \code{avg\_weekly\_clicks} gần
        như đo lường cùng một khía cạnh --- đưa cả hai vào mô hình có thể gây
        \textbf{đa cộng tuyến}. \code{active\_weeks} có
        tương quan khá cao với \code{num\_submitted}, nhưng số click mỗi tuần
        lại gần như không liên quan đến số bài nộp --- cho thấy lượng click
        không phải lúc nào cũng phản ánh việc học có mục đích.
  \item \textbf{Cặp nghịch chiều rõ nhất} là \code{avg\_score} và
        \code{num\_failed}: điểm trung bình cao đi kèm số bài trượt thấp, do đó
        có thể cân nhắc chỉ giữ lại một trong hai đặc trưng.
  \item \code{avg\_days\_early} tương quan thấp với hầu hết các đặc trưng còn
        lại --- đây là một tín hiệu tương đối độc lập, phản ánh thói quen quản
        lý thời gian của sinh viên.
  \item \code{num\_failed} gần như không tương quan với các đặc trưng hoạt động
        --- tương tác nhiều hay học đều đặn không bảo đảm tránh được điểm kém;
        kết quả còn phụ thuộc vào chất lượng của việc học.
\end{itemize}

Quan hệ giữa \code{avg\_score} và \code{num\_failed} được thể hiện rõ hơn qua
biểu đồ bong bóng ở Hình~\ref{fig:bubble}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\textwidth]{bubble_score_failed.png}
  \caption{Biểu đồ bong bóng: \code{avg\_score} theo \code{num\_failed}.}
  \label{fig:bubble}
\end{figure}

Phần lớn sinh viên không trượt bài nào (\code{num\_failed} $= 0$) và có
\code{avg\_score} tập trung ở mức cao, rõ nhất trong vùng 80--100 điểm. Khi số
bài trượt tăng, số sinh viên giảm dần và điểm trung bình phân tán hơn. Tuy
nhiên, ở các mức \code{num\_failed} thấp (1--5) vẫn có những sinh viên đạt điểm
khá cao. Đây là lý do hai đặc trưng này không thể thay thế cho nhau:
\code{num\_failed} nắm bắt được một dạng rủi ro mà điểm trung bình bỏ sót ---
sinh viên học ổn định nhưng bỏ hoặc trượt một vài bài, có thể do thiếu chuyên
cần hoặc do hoàn cảnh cá nhân.

\subsection{Đặc trưng động theo nhãn mục tiêu}

Biểu đồ radar các đặc trưng động theo nhóm kết quả ở Hình~\ref{fig:radar_dyn}
làm rõ hơn sự khác biệt giữa các nhóm.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{radar_dynamic.png}
  \caption{Biểu đồ radar các đặc trưng động theo nhóm kết quả.}
  \label{fig:radar_dyn}
\end{figure}

Giá trị trung bình các đặc trưng động theo nhóm kết quả được trình bày ở
Bảng~\ref{tab:mean_dynamic}.

\input{table_mean_dynamic}

\begin{itemize}[leftmargin=*]
  \item \textbf{Pass} chiếm diện tích lớn nhất, vượt trội ở cả ba đặc trưng
        tương tác VLE: \code{active\_weeks}, \code{total\_clicks},
        \code{avg\_weekly\_clicks}, cùng với số bài nộp. Đây là nhóm duy nhất vừa
        duy trì mức độ tham gia cao, vừa đạt kết quả bài đánh giá tốt.
  \item \textbf{Fail} nổi bật nhất ở số bài trượt \code{num\_failed}, cao hơn
        cả hai nhóm còn lại, trong khi số bài nộp tương đương nhóm Pass. Như
        vậy, sinh viên Fail vẫn nộp bài đầy đủ nhưng kết quả không đạt yêu cầu
        --- dấu hiệu của khó khăn học tập kéo dài.
  \item \textbf{Withdrawn} thì ngược lại: số bài nộp thấp nhất trong ba nhóm,
        còn số bài trượt gần như bằng 0. Điều này không có nghĩa là họ làm bài
        tốt --- họ nộp quá ít bài nên hầu như không có bài nào bị tính trượt.
        Đáng chú ý, những bài đã nộp vẫn đạt điểm ở mức trung bình khá, cho
        thấy nhóm này bỏ học không hẳn vì học kém, mà nhiều khả năng do mất dần
        sự gắn kết với khoá học.
\end{itemize}

Cuối cùng, xét \code{avg\_days\_early} theo nhóm kết quả ở
Hình~\ref{fig:days_early}: Pass là nhóm duy nhất có giá trị dương, $+1.59$
ngày, tức nộp trước hạn, thể hiện thái độ học tập chủ động. Hai nhóm còn lại
đều mang giá trị âm, trong đó Withdrawn nộp trễ nhiều nhất, $-0.91$ ngày, gấp
gần 5 lần mức $-0.19$ ngày của Fail; kết quả này phù hợp với xu hướng giảm dần
sự gắn kết đã ghi nhận ở trên. Tuy vậy, mức chênh lệch giữa ba nhóm đều nhỏ,
dưới 2 ngày, nên khi đứng một mình, \code{avg\_days\_early} có thể chưa đủ
mạnh để phân biệt các nhóm.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{avg_days_early.png}
  \caption{Giá trị trung bình \code{avg\_days\_early} theo nhóm kết quả.}
  \label{fig:days_early}
\end{figure}

% =====================================================================
%  CHƯƠNG 3 — XÂY DỰNG VÀ SO SÁNH MÔ HÌNH
% =====================================================================
\chapter{Xây dựng và so sánh mô hình}
\label{part:model}

\section{Tiền xử lý dữ liệu}

Dựa trên cơ sở lý thuyết ở Chương~\ref{part:theory} và các kết quả phân tích
khám phá dữ liệu ở Chương~\ref{part:eda}, chương này trình bày quy trình tiền
xử lý dữ liệu, xây dựng đặc trưng, huấn luyện và đánh giá mô hình cho bài toán
cảnh báo sớm rủi ro học tập. Bài toán được chuẩn hóa thành \textbf{phân loại
nhị phân}: nhóm \textbf{At-risk} được hình thành bằng cách gộp hai kết cục
\textbf{Fail} và \textbf{Withdrawn}, đối lập với nhóm \textbf{Pass}. Dự báo được thực hiện dựa trên
các đặc trưng nhân khẩu học và hành vi học tập tích lũy đến từng mốc quan sát
trong khóa học.

\textbf{Điền giá trị thiếu ở các đặc trưng động.} Các đặc trưng phản ánh hành
vi học tập tích lũy, gồm tương tác với hệ thống học trực tuyến và kết quả bài
đánh giá, có thể bị thiếu khi sinh viên chưa phát sinh hoạt động hoặc chưa nộp
bài tại thời điểm quan sát. Vì ý nghĩa của giá trị thiếu khác nhau giữa các
nhóm biến, quá trình điền khuyết được thực hiện theo từng trường hợp:

\begin{itemize}[leftmargin=*]
  \item \textbf{Đặc trưng tương tác VLE}
        (\code{total\_clicks},
        \code{active\_weeks},
        \code{avg\_weekly\_clicks}) và \textbf{đặc trưng bài đánh giá}
        (\code{num\_submitted},
        \code{num\_failed}) được điền bằng 0, vì giá trị thiếu trong trường
        hợp này biểu thị việc sinh viên chưa phát sinh hoạt động tương ứng.
  \item \code{avg\_score} được điền bằng giá trị \textbf{-1}, nằm ngoài miền
        điểm hợp lệ $[0,100]$, để phân biệt với trường hợp sinh viên đã nộp
        bài nhưng đạt 0 điểm.
  \item \code{avg\_days\_early} được điền bằng 0. Việc sinh viên chưa nộp bài
        dù đã đến hạn đã được phản ánh bởi biến nhị phân
        \code{no\_submission\_despite\_due}, do đó cách điền này không làm mất
        thông tin.
\end{itemize}

Sau bước điền khuyết, toàn bộ đặc trưng động không còn giá trị thiếu. Tuy
nhiên, dữ liệu nhân khẩu học vẫn có một dạng thiếu đặc biệt cần được xử lý
riêng.

\textbf{Xử lý giá trị thiếu của \code{imd\_band}.} Như đã nêu ở
Mục~\ref{sec:imd} ở Chương~\ref{part:eda}, biến \code{imd\_band} sử dụng ký hiệu
\code{'?'} để biểu diễn giá trị
thiếu thay vì giá trị rỗng thông thường. Trước khi điền khuyết, biến chỉ báo
\code{imd\_missing} được tạo để lưu lại trạng thái thiếu dữ liệu, qua đó cho
phép mô hình khai thác bản thân hiện tượng thiếu như một tín hiệu dự báo.

Giá trị thiếu của \code{imd\_band} được điền theo phân phối của từng vùng
\code{region}. Phân phối này được ước lượng trên tập huấn luyện và áp dụng
thống nhất cho tập kiểm tra nhằm tránh rò rỉ thông tin. Sau xử lý, biến
\code{imd\_band\_filled} không còn giá trị thiếu ở cả hai tập dữ liệu.

\textbf{Mã hóa đặc trưng phân loại.} Sau khi hoàn tất điền khuyết, các đặc
trưng phân loại được chuyển sang dạng số theo bản chất đo lường của từng biến.
Phương án mã hóa cụ thể được trình bày trong Bảng~\ref{tab:encoding}.

\begin{table}[H]
  \centering
  \caption{Phương án mã hóa các đặc trưng phân loại.}
  \label{tab:encoding}
  \small
  \begin{tabular}{>{\raggedright\arraybackslash}p{0.28\textwidth}
                  >{\raggedright\arraybackslash}p{0.20\textwidth}
                  >{\raggedright\arraybackslash}p{0.38\textwidth}}
    \toprule
    \textbf{Đặc trưng} & \textbf{Kiểu mã hóa} & \textbf{Lý do} \\
    \midrule
    \code{disability}         & Nhị phân & Y $\to$ 1, N $\to$ 0 \\
    \code{highest\_education} & Thứ tự & Có thứ bậc học vấn rõ ràng \\
    \code{imd\_band\_filled}  & Thứ tự & Có thứ bậc mức độ thiếu thốn kinh tế \\
    \bottomrule
  \end{tabular}
\end{table}

Ba đặc trưng nhân khẩu học còn lại trong dữ liệu gốc gồm \code{gender},
\code{region} và \code{age\_band} không được đưa vào tập đặc trưng huấn luyện.
Ngược lại, \code{highest\_education}, \code{disability} và
\code{imd\_band\_filled} được giữ lại vì đã thể hiện mối liên hệ rõ ràng với
kết quả học tập trong Chương~\ref{part:eda}.

\textbf{Tập đặc trưng cuối cùng.} Sau tiền xử lý, tập dữ liệu dùng để huấn
luyện mô hình gồm \textbf{15 đặc trưng}: 6 đặc trưng tĩnh và 9 đặc trưng động.
Ý nghĩa của từng đặc trưng được tóm tắt trong Bảng~\ref{tab:final_features}.

\input{table_final_features}

Dữ liệu được chia thành tập huấn luyện và tập kiểm tra theo từng sinh viên,
giữ nguyên thiết kế đã trình bày ở Bảng~\ref{tab:split}
trong Chương~\ref{part:eda} với tỷ lệ 80:20. Cách chia này bảo đảm các bản ghi của
cùng một sinh viên không xuất hiện đồng thời ở cả hai tập.

\textbf{Chuẩn hóa bài toán về phân loại nhị phân.} Với mục tiêu cảnh báo sớm,
ranh giới quan trọng nhất là giữa nhóm sinh viên hoàn thành và không hoàn
thành học phần, thay vì tách riêng hai kết cục Fail và Withdrawn. Vì vậy, nhãn
mục tiêu được xác định lại như sau:

\[
  \text{At-risk (1)} = \text{Fail} \cup \text{Withdrawn}, \qquad
  \text{Pass (0)} = \text{Pass}.
\]

Phép gộp nhãn được áp dụng thống nhất trên toàn bộ dữ liệu dạng panel, tức với
tất cả bản ghi tại tám mốc dự đoán
$T \in \{30,60,\ldots,240\}$ được xây dựng ở
Mục~\ref{sec:panel_design} thuộc Chương~\ref{part:eda}. Sau khi hoàn tất tiền xử
lý và chuẩn hóa nhãn, dữ liệu được sử dụng cho giai đoạn huấn luyện, so sánh
và diễn giải các mô hình học máy ở các mục tiếp theo.

\section{Khảo sát sơ bộ theo các mốc dự đoán}
\label{sec:snapshot_survey}

Trước khi so sánh nhiều thuật toán tại một mốc thời gian cố định, cần khảo sát
sơ bộ hiệu suất mô hình \textbf{trên toàn bộ các mốc dự đoán} đã thiết kế ở
Mục~\ref{sec:panel_design} ở Chương~\ref{part:eda}. Phân tích này nhằm hai mục
đích: thứ nhất, đánh giá xu hướng thay đổi hiệu suất khi sinh viên tích luỹ thêm dữ
liệu hành vi theo thời gian; thứ hai, lựa chọn một mốc đại diện cho các thử
nghiệm chuyên sâu ở phần sau.

\subsection{Thiết lập thử nghiệm}

Với mỗi mốc dự đoán $T$, một mô hình \textbf{Logistic Regression} được huấn
luyện riêng trên dữ liệu của đúng mốc đó. Đây là thuật toán cơ sở, đơn giản và
dễ diễn giải nhất trong nhóm thuật toán được trình bày ở
Chương~\ref{part:theory},
phù hợp để khảo sát nhanh xu hướng hiệu suất theo thời gian trước khi mở rộng
sang các mô hình phức tạp hơn. Quy trình tinh chỉnh cho từng mốc tương tự cách
tiếp cận ở giai đoạn so sánh thuật toán tại Mục~\ref{sec:methodology}: dữ liệu
được chuẩn hoá bằng \code{StandardScaler}; thao tác fit chỉ thực hiện trên
train-fold để tránh rò rỉ dữ liệu. Siêu tham số regularization $C$ của mô hình
ở Mục~\ref{sec:logreg_reg}, tỷ lệ oversampling SMOTE và trọng số lớp At-risk
được tinh chỉnh đồng thời bằng Optuna với 30 lượt thử nghiệm và kiểm định chéo
phân tầng 3-fold. Sau đó, mô hình được huấn luyện lại trên toàn bộ tập huấn
luyện của mốc tương ứng và đánh giá trên tập kiểm tra giữ nguyên.

Về mặt phương pháp, thử nghiệm ban đầu sử dụng \textit{recall thuần} của lớp
At-risk làm hàm mục tiêu. Tuy nhiên, lựa chọn này khiến mô hình suy biến về
phương án dự đoán \textbf{toàn bộ} sinh viên là At-risk: recall gần tuyệt đối
nhưng precision rất thấp, do đó không có giá trị vận hành. Vì vậy, hàm mục
tiêu được chuyển sang \textbf{F1-score của lớp At-risk}, buộc mô hình cân bằng
giữa precision và recall. Hàm mục tiêu này được giữ nguyên trong toàn bộ các
thử nghiệm còn lại của báo cáo.

\subsection{Kết quả theo thời gian}

Bảng~\ref{tab:snapshot_results} tổng hợp hiệu suất trên tập kiểm tra của tám
mô hình Logistic Regression, tương ứng với tám mốc dự đoán.

\input{table_snapshot_results}

Hình~\ref{fig:trend_snapshot} trực quan hoá xu hướng của bốn chỉ số theo thời
gian.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.85\textwidth]{trend_by_snapshot.png}
  \caption{Xu hướng hiệu suất mô hình theo từng mốc dự đoán.}
  \label{fig:trend_snapshot}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item \textbf{Accuracy} và \textbf{precision At-risk} tăng \textbf{đơn điệu}
        theo thời gian, không có mốc nào giảm so với mốc liền trước. Accuracy
        tăng từ 0.6488 ở $T=30$ lên 0.8796 ở $T=240$, trong khi precision tăng
        từ 0.5750 lên 0.8269. Kết quả này dễ giải thích: sinh viên càng tích
        luỹ nhiều hành vi học tập, tín hiệu dự báo càng rõ ràng và đáng
        tin cậy.
  \item \textbf{Recall At-risk} lại dao động trong một biên độ hẹp
        trong khoảng 0.7555--0.8013 mà không theo xu hướng rõ ràng, thậm chí
        đạt giá trị cao nhất 0.8013 ngay ở mốc sớm nhất $T=30$, khi độ chính
        xác tổng thể còn thấp với accuracy 0.6488 và precision 0.5750. Điều này cho thấy ở giai
        đoạn đầu khoá, khi tín hiệu hành vi còn thưa thớt, ranh giới tuyến tính
        của Logistic Regression có xu hướng dự báo At-risk tương đối rộng:
        mô hình phát hiện được phần lớn sinh viên có nguy cơ nhưng phải đánh
        đổi bằng nhiều cảnh báo nhầm.
  \item \textbf{F1 At-risk} --- chỉ số tổng hợp được dùng làm mục tiêu tối ưu
        --- cũng tăng đều theo thời gian, từ 0.6695 tại $T=30$ lên 0.8035 tại
        $T=240$. Tuy nhiên, mức tăng này chủ yếu đến từ \textbf{precision},
        trong khi recall gần như ổn định xuyên suốt khoá học.
\end{itemize}

Ma trận nhầm lẫn của bốn mốc tiêu biểu --- 30, 90, 180 và 240 ngày --- được thể
hiện ở Hình~\ref{fig:cm_snapshot}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.75\textwidth]{cm_grid_snapshots.png}
  \caption{Ma trận nhầm lẫn trên tập kiểm tra tại bốn mốc dự đoán tiêu biểu ---
  30, 90, 180 và 240 ngày.}
  \label{fig:cm_snapshot}
\end{figure}

Bảng~\ref{tab:snapshot_params} cho thấy siêu tham số xử lý mất cân bằng nhãn
mà Optuna lựa chọn theo từng mốc thời gian.

\input{table_snapshot_params}

Trọng số At-risk tối ưu hầu như luôn hội tụ gần giá trị 1, trong khoảng
1.01--1.33 ở mọi
mốc thời gian, trong khi tỷ lệ SMOTE dao động ở mức khá cao và không có xu
hướng rõ rệt, từ 0.42x đến 0.90x. Nói cách khác, đối với Logistic Regression, việc
bổ sung mẫu tổng hợp bằng SMOTE đóng vai trò chủ đạo hơn so với điều chỉnh
trọng số lớp trong xử lý mất cân bằng nhãn. Đây là đặc điểm nhất quán của mô
hình này trên các mốc thời gian khảo sát.

\subsection{Lựa chọn mốc dự đoán cho phân tích chuyên sâu}
\label{sec:choose_t150}

Kết quả khảo sát cho thấy sự đánh đổi rõ ràng giữa \textbf{độ tin cậy của dự
báo} và \textbf{thời gian còn lại để can thiệp}: mốc dự đoán càng muộn thì
hiệu suất càng cao, nhưng cảnh báo cũng càng gần thời điểm kết thúc khoá học,
làm giảm giá trị thực tiễn của hệ thống. Với các khoá học OULAD kéo dài khoảng
240--270 ngày, hai mốc có F1 cao nhất --- 210 và 240 ngày, lần lượt 0.7935 và
0.8035 --- chỉ còn khoảng 10--20\% thời lượng khoá học. Đây là thời điểm quá
muộn để nhà trường triển khai các biện pháp hỗ trợ có ý nghĩa.

Mốc \textbf{150 ngày} được chọn làm đại diện cho các thử nghiệm chuyên sâu ở
phần còn lại của báo cáo vì đạt cân bằng hợp lý giữa hai yêu cầu trên. Về hiệu
suất, mốc này cho kết quả tương đối cao với accuracy 0.8263 và F1 At-risk 0.7591,
và chỉ thấp hơn khoảng 0.016 so với mốc 180 ngày liền sau, một khoảng cách nhỏ
so với lợi ích thu được về thời gian. Về khả năng can thiệp, cảnh báo tại ngày
thứ 150 vẫn còn khoảng 90--120 ngày, tương đương 37--50\% thời lượng khoá học,
đủ để nhà trường tổ chức các hoạt động hỗ trợ có chiều sâu thay vì chỉ nhắc
nhở gấp rút ở giai đoạn cuối.

Bảng~\ref{tab:split_t150} tóm tắt quy mô và phân phối nhãn tại mốc 150 ngày
sau khi gộp nhãn, được dùng thống nhất cho toàn bộ các thử nghiệm từ
Mục~\ref{sec:methodology} trở đi.

\input{table_split_t150}

Tỷ lệ At-risk chiếm khoảng \textbf{36\%} tổng số sinh viên ở cả hai tập và gần
như tương đồng giữa train và test. Điều này xác nhận rằng cách chia dữ liệu
theo sinh viên không gây lệch phân phối nhãn.

\section{Phương pháp xây dựng mô hình}
\label{sec:methodology}

Từ kết luận ở Mục~\ref{sec:choose_t150}, các thử nghiệm tiếp theo tập trung
vào mốc 150 ngày. Phân tích được mở rộng từ mô hình cơ sở Logistic Regression,
đã dùng để khảo sát xu hướng theo thời gian ở Mục~\ref{sec:snapshot_survey},
sang so sánh trực tiếp các thuật toán học máy khác nhau.

\medskip
\noindent\textbf{Xử lý mất cân bằng nhãn.} Hai kỹ thuật được kết hợp để xử lý
mất cân bằng nhãn:

\begin{itemize}[leftmargin=*]
  \item \textbf{SMOTE}: sinh thêm
        mẫu tổng hợp cho lớp At-risk. Tỷ lệ oversampling được xem là một siêu
        tham số và được tinh chỉnh riêng cho từng mô hình.
  \item \textbf{Trọng số lớp} \code{class\_weight}: tăng chi phí phân loại
        sai đối với lớp At-risk trong hàm mất mát của mô hình. Đại lượng này
        cũng được tinh chỉnh như một siêu tham số và tương ứng về mặt toán học
        với hàm mất mát có trọng số đã trình bày ở
        Mục~\ref{sec:logreg_weight} ở Chương~\ref{part:theory} đối với
        Logistic Regression.
\end{itemize}

Để tránh rò rỉ dữ liệu, \textbf{SMOTE chỉ được áp dụng trên từng fold huấn
luyện} trong kiểm định chéo, không áp dụng lên fold kiểm định. Tương tự, với
Logistic Regression --- mô hình duy nhất cần chuẩn hoá dữ liệu số bằng
\code{StandardScaler} --- tham số chuẩn hoá cũng chỉ được fit trên fold huấn
luyện.

\medskip
\noindent\textbf{Thiết lập thực nghiệm.} Sau khi cố định nguyên tắc xử lý mất
cân bằng nhãn và kiểm soát rò rỉ dữ liệu,
quy trình thực nghiệm được thiết lập thống nhất cho toàn bộ mô hình. Với mỗi
mô hình, siêu tham số của thuật toán được tinh chỉnh \textbf{đồng thời}
với tỷ lệ SMOTE và trọng số lớp At-risk bằng khung tối ưu Bayesian
\textbf{Optuna} với thuật toán lấy mẫu TPE, với 30 lượt thử nghiệm cho mỗi mô
hình, theo nguyên lý đã trình bày ở Mục~\ref{sec:bayes_opt}. Hàm mục tiêu là
\textbf{F1-score của lớp At-risk}, được đánh giá bằng kiểm định chéo phân tầng
3-fold bằng \code{StratifiedKFold} để giữ nguyên tỷ lệ lớp ở mỗi fold. Sau khi xác định
bộ siêu tham số tốt nhất, mô hình được huấn luyện lại trên toàn bộ tập huấn
luyện và đánh giá lần cuối trên tập kiểm tra holdout, độc lập với
quá trình tinh chỉnh.

Các chỉ số đánh giá trên tập kiểm tra gồm accuracy, precision, recall, F1 của
lớp At-risk và ROC-AUC.

\section{Kết quả trên bộ đặc trưng đầy đủ}
\label{sec:compare_full}

Bảng~\ref{tab:compare_full} tổng hợp hiệu suất của bốn mô hình trên tập kiểm
tra khi sử dụng đầy đủ 15 đặc trưng.

\input{table_compare_full}

\begin{itemize}[leftmargin=*]
  \item \textbf{CatBoost} dẫn đầu ở ba chỉ số: accuracy 0.8501, precision
        At-risk 0.8055 và F1 At-risk 0.7849. Tuy nhiên, recall lại đạt giá trị thấp nhất,
        thậm chí thấp hơn cả Logistic Regression.
  \item \textbf{LightGBM} đạt ROC-AUC cao nhất 0.9116, nhưng chênh lệch so
        với CatBoost, 0.9114, là không đáng kể; F1 At-risk của mô hình này,
        0.7827, cũng chỉ thấp hơn CatBoost khoảng 0.2%.
  \item \textbf{Random Forest} có recall At-risk cao nhất 0.7819, tức bỏ sót
        ít sinh viên có nguy cơ nhất, nhưng phải đánh đổi bằng precision thấp
        hơn CatBoost. Nhìn chung, ba mô hình cây cho kết quả rất sát nhau với
        F1 nằm trong khoảng hẹp 0.781--0.785; không mô hình nào vượt trội rõ
        rệt.
  \item \textbf{Logistic Regression} có hiệu suất thấp nhất ở hầu hết các chỉ số, trong khi 
        các mô hình bagging và boosting đều cho kết quả tốt hơn, cho thấy khả năng khai thác 
        các quan hệ phức tạp trong dữ liệu hiệu quả hơn.
\end{itemize}

Ma trận nhầm lẫn của từng mô hình trên tập kiểm tra được thể hiện ở
Hình~\ref{fig:cm_full}.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{cm_logreg_full.png}
    \caption{Logistic Regression}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{cm_rf_full.png}
    \caption{Random Forest}
  \end{subfigure}\\[0.6em]
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{cm_catboost_full.png}
    \caption{CatBoost}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{cm_lgbm_full.png}
    \caption{LightGBM}
  \end{subfigure}
  \caption{Ma trận nhầm lẫn trên tập kiểm tra --- bộ đặc trưng đầy đủ, tại mốc
  150 ngày.}
  \label{fig:cm_full}
\end{figure}

Nhìn chung, cả bốn mô hình đều phân loại tốt hai nhóm Pass và At-risk, với số dự đoán đúng
chiếm phần lớn. CatBoost nhận diện nhóm Pass tốt nhất với 2.516 trường hợp đúng,
nhưng đồng thời bỏ sót nhiều sinh viên At-risk nhất (366 trường hợp). Ngược lại, Random Forest
nhận diện nhóm At-risk tốt nhất với 1.219 trường hợp đúng và chỉ bỏ sót 340 trường hợp. 
LightGBM cho kết quả khá cân bằng giữa hai nhóm - tương tự như Random Forest, 
trong khi Logistic Regression có số lỗi phân loại tổng thể cao nhất.

\section{Độ quan trọng và rút gọn đặc trưng}
\label{sec:feat_importance}

Phần này trả lời hai câu hỏi liên tiếp. Trước hết, cần xác định những đặc trưng
nào đóng góp ổn định nhất cho dự báo At-risk. Sau đó, từ nhóm đặc trưng quan
trọng này, cần kiểm tra liệu mô hình có thể duy trì hiệu suất khi chỉ sử dụng
một bộ đặc trưng gọn hơn hay không.

Hình~\ref{fig:feat_imp} thể hiện độ quan trọng đặc trưng theo định nghĩa riêng
của từng thuật toán: hệ số hồi quy tuyệt đối với Logistic Regression, độ giảm
tạp chất Gini với Random Forest theo công thức ở
Mục~\ref{sec:rf_importance}, PredictionValuesChange với CatBoost, và số lần
được dùng để phân tách với LightGBM. Do các giá trị này \textbf{không cùng đơn
vị}, phân tích chỉ dùng để so sánh thứ hạng đặc trưng trong từng mô hình, không
so sánh trực tiếp độ lớn giữa các mô hình.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{feature_importance_native.png}
  \caption{Độ quan trọng đặc trưng theo định nghĩa riêng của từng mô hình tại
  mốc 150 ngày.}
  \label{fig:feat_imp}
\end{figure}

Để xây dựng một thứ hạng chung, mỗi đặc trưng được xếp hạng riêng trong từng
mô hình, sau đó lấy trung bình cộng bốn hạng. Bảng~\ref{tab:feature_rank}
trình bày kết quả, sắp xếp tăng dần theo hạng trung bình.

\input{table_feature_rank}

\begin{itemize}[leftmargin=*]
  \item \textbf{Nhóm dẫn đầu} --- \code{num\_submitted\_filled} đứng hạng 1 ở
        Logistic Regression và CatBoost, hạng 2 ở Random Forest và LightGBM,
        với hạng trung bình 1.50. Đây là tín hiệu dự báo mạnh và nhất quán
        nhất. Theo sau là \code{avg\_score\_filled}, hạng trung bình 2.50 và
        dẫn đầu ở Random Forest cùng LightGBM, rồi đến \code{num\_due} và
        \code{active\_weeks\_filled} cùng ở hạng trung bình 5.00. Cả bốn đều là
        đặc trưng \textbf{động}, phản ánh trực tiếp mức độ tham gia và kết quả
        học tập tích luỹ của sinh viên.
  \item \textbf{Nhóm giữa} gồm các đặc trưng tương tác VLE còn lại:
        \begingroup\raggedright
        \code{total\_clicks\_filled}, \code{avg\_weekly\_clicks\_filled},
        \code{avg\_days\_early\_filled} và \code{highest\_education} --- đóng
        góp bổ sung nhưng không mang tính quyết định.
        \par\endgroup
  \item \textbf{Nhóm cuối} --- \code{studied\_credits}, \code{disability},
        \code{imd\_missing} có hạng trung bình cao nhất, tức ít quan trọng nhất,
        cho thấy các đặc trưng tĩnh mang tính nhân khẩu học có vai trò dự báo
        khiêm tốn hơn đáng kể so với các đặc trưng hành vi động.
\end{itemize}

\textbf{Kiểm tra trên các bộ đặc trưng rút gọn.} Từ thứ hạng ở
Bảng~\ref{tab:feature_rank}, mười đặc trưng quan trọng nhất (Top-10) và năm đặc
trưng quan trọng nhất (Top-5) được chọn để huấn luyện lại theo đúng quy trình
tinh chỉnh đã mô tả ở Mục~\ref{sec:methodology}. Mục tiêu là kiểm tra liệu hiệu
suất mô hình có \textbf{ổn định} khi giảm số lượng đặc trưng đầu vào hay không.
Đây là yếu tố quan trọng trong triển khai thực tế, vì một hệ thống cảnh báo sử
dụng ít đặc trưng hơn sẽ đơn giản hơn trong thu thập dữ liệu, vận hành và bảo
trì.

Phân tích rút gọn tập trung vào \textbf{CatBoost}, mô hình đạt hiệu suất tốt
nhất trong bước so sánh thuật toán ở Mục~\ref{sec:compare_full}. Việc thu hẹp
về một mô hình duy nhất giúp câu hỏi đánh giá trở nên cụ thể hơn: với mô hình
dự kiến triển khai, có thể giảm bao nhiêu đặc trưng mà hiệu suất vẫn gần như
được duy trì? Bảng~\ref{tab:compare_featuresets} đặt cạnh nhau kết quả của
CatBoost trên ba bộ đặc trưng.

\input{table_compare_featuresets}

\begin{itemize}[leftmargin=*]
  \item Hiệu suất \textbf{gần như không đổi} trên cả ba bộ đặc trưng: F1
        At-risk chỉ dao động trong khoảng $0.7825$--$0.7875$, trong khi accuracy cũng chỉ 
        chênh $0.004$. Kết quả này cho thấy phần lớn sức mạnh dự báo tập trung
        ở một nhóm nhỏ đặc trưng hành vi động, đúng với thứ hạng đã quan sát ở
        Bảng~\ref{tab:feature_rank}.
  \item Đáng chú ý, bộ \textbf{Top-10 đạt kết quả cao hơn bộ đầy đủ} ở ba trên
        năm chỉ số: F1, recall và ROC-AUC. Cụ thể, F1 tăng từ $0.7849$ lên
        $0.7875$, recall tăng từ $0.7652$ lên $0.7774$, còn ROC-AUC tăng từ
        $0.9114$ lên $0.9119$, trong khi sử dụng ít
        hơn một phần ba số đặc trưng. Việc loại bỏ năm đặc trưng kém quan
        trọng nhất không những không làm giảm hiệu suất mà còn giúp mô hình
        phát hiện thêm sinh viên có nguy cơ. Điều này gợi ý rằng nhóm đặc
        trưng bị loại chủ yếu đóng góp nhiễu hoặc thông tin dư thừa.
  \item Khi tiếp tục rút gọn xuống \textbf{Top-5}, hiệu suất bắt đầu giảm nhẹ
        nhưng vẫn ở mức không đáng kể: F1 thấp hơn bộ đầy đủ đúng $0.0024$ và
        thấp hơn Top-10 $0.005$. Đổi lại, số đặc trưng cần thu thập giảm đáng kể từ
        15 xuống 5.
  \item Xu hướng chung khi giảm đặc trưng là \textbf{precision giảm, recall
        tăng}: precision giảm từ $0.8055$ ở Full xuống $0.7910$ ở Top-5, trong
        khi recall tăng nhẹ từ $0.7652$ lên $0.7742$. Với bài toán cảnh báo
        sớm, đây là hướng đánh đổi có lợi, vì bỏ sót một sinh viên cần hỗ trợ
        thường gây hậu quả lớn hơn một cảnh báo nhầm.
\end{itemize}

Tổng hợp lại, \textbf{bộ Top-5 là lựa chọn hợp lý cho triển khai thực tế}. Bộ
này chỉ cần năm đặc trưng --- \code{num\_submitted\_filled},
\code{avg\_score\_filled}, \code{num\_due}, \code{active\_weeks\_filled},
\code{total\_clicks\_filled} --- toàn bộ đều là hành vi học tập và không yêu
cầu thông tin nhân khẩu học, nhưng vẫn giữ gần như trọn vẹn hiệu suất của mô
hình dùng đủ 15 đặc trưng. Nếu ưu tiên hiệu suất cao nhất và chấp nhận thu
thập nhiều dữ liệu hơn, Top-10 là phương án phù hợp hơn.

\section{Hiệu chỉnh ngưỡng và độ tin cậy của kết quả}
\label{sec:threshold_bootstrap}

Hai phân tích trong phần này bổ sung cho các kết quả đã trình bày và đều được
thực hiện trên \textbf{CatBoost với bộ 15 đặc trưng đầy đủ}, cấu hình đạt hiệu
suất tốt nhất ở Mục~\ref{sec:compare_full}. Phân tích thứ nhất xem xét câu hỏi
vận hành: có thể phát hiện thêm bao nhiêu sinh viên có nguy cơ nếu chấp nhận
thêm cảnh báo nhầm. Phân tích thứ hai đánh giá độ tin cậy của các chỉ số hiệu
suất thông qua khoảng tin cậy bootstrap.

\subsection{Hiệu chỉnh ngưỡng phân loại}

Các kết quả từ đầu chương đến thời điểm này đều sử dụng \textbf{ngưỡng mặc
định 0.5}: sinh viên được gán nhãn At-risk khi xác suất dự báo vượt quá 0.5.
Ngưỡng này gần với điểm tối đa hoá F1; F1 đạt đỉnh tại 0.53. Tuy nhiên, F1 mặc
định xem precision và recall có tầm quan trọng ngang nhau. Giả định này chưa
phù hợp hoàn toàn với bài toán cảnh báo sớm, vì bỏ sót một sinh viên đang gặp
rủi ro thường nghiêm trọng hơn việc tạo ra một cảnh báo nhầm cần cố vấn học
tập rà soát. 

Hình~\ref{fig:threshold_sweep} theo dõi ba chỉ số khi ngưỡng thay đổi từ 0.05
đến 0.95.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.92\textwidth]{threshold_sweep.png}
  \caption{Precision, recall và F1 của lớp At-risk theo ngưỡng phân loại.}
  \label{fig:threshold_sweep}
\end{figure}

Điểm đáng chú ý nhất là \textbf{F1 gần như ổn định} trong một dải ngưỡng rộng,
khoảng 0.35--0.60, trong khi precision và recall thay đổi theo hai hướng
ngược nhau. Nói cách khác, có thể giảm ngưỡng phân loại mà chất lượng tổng hợp
của mô hình gần như không suy giảm, đổi lại hệ thống ưu tiên phát hiện nhiều
sinh viên có nguy cơ hơn.

Bảng~\ref{tab:threshold} và Hình~\ref{fig:cm_threshold} lượng hoá sự đánh đổi
này bằng số sinh viên cụ thể.

\input{table_threshold}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{cm_by_threshold.png}
  \caption{Ma trận nhầm lẫn trên tập kiểm tra tại bốn mức ngưỡng.}
  \label{fig:cm_threshold}
\end{figure}

Khi giảm ngưỡng từ 0.5 xuống 0.4, số sinh viên At-risk bị bỏ sót giảm từ 366
xuống 297, tức \textbf{phát hiện thêm 69 sinh viên có nguy cơ}, trong khi F1
chỉ giảm $0.0068$, từ $0.7849$ xuống $0.7781$. Đổi lại, hệ thống phát sinh
thêm 135 cảnh báo nhầm. Nếu tiếp tục hạ ngưỡng xuống 0.2, số sinh viên bị bỏ
sót chỉ còn 128, nhưng số cảnh báo nhầm tăng lên 1.001; khi đó danh sách cảnh
báo trở nên quá rộng và precision chỉ còn $0.5884$.

Bảng~\ref{tab:target_recall} tiếp cận theo chiều ngược lại: đặt ra mức recall
mong muốn rồi tính ngưỡng cần dùng và mức đánh đổi tương ứng.

\input{table_target_recall}

Bốn cột cuối cho thấy khi đặt mục tiêu recall cao hơn, số sinh viên At-risk bị
bỏ sót giảm dần, nhưng danh sách cảnh báo cũng mở rộng nhanh. Ở mức recall mục
tiêu 80\%, hệ thống cần thêm 94 cảnh báo nhầm để phát hiện thêm 55 sinh viên
At-risk, tương đương khoảng 1{,}7 cảnh báo nhầm cho mỗi ca phát hiện thêm. Khi
nâng mục tiêu lên 95\%, hệ thống phát hiện thêm 290 sinh viên nhưng đồng thời
phát sinh thêm 1.001 cảnh báo nhầm, tức khoảng 3{,}5 cảnh báo nhầm cho mỗi ca
phát hiện thêm. Vì vậy, việc tăng recall không chỉ là bài toán chọn ngưỡng thấp
hơn, mà còn là quyết định về số lượng cảnh báo mà nhà trường có thể kiểm tra và
hỗ trợ thực tế.

Dựa vào các kết quả trên có thể thấy rằng vùng ngưỡng \textbf{0.40--0.43} là điểm 
cân bằng hợp lý: recall đạt khoảng 80\%, precision vẫn trên $0.75$ (tức khoảng ba trong bốn
cảnh báo là chính xác) và F1 gần như không đổi so với ngưỡng mặc định.


\subsection{Khoảng tin cậy bootstrap của các chỉ số}

Mọi chỉ số trong báo cáo đều là \textbf{ước lượng điểm} tính trên một tập kiểm
tra hữu hạn gồm 4.363 bản ghi. Nếu đánh giá trên một tập kiểm tra khác, các
chỉ số này có thể thay đổi. Vì vậy, cần ước lượng mức độ dao động có thể xảy
ra quanh các giá trị đã báo cáo.

Phương pháp \textbf{bootstrap} cho phép ước lượng độ bất định này mà không cần
thu thập thêm dữ liệu. Cụ thể, tập kiểm tra được lấy mẫu lại \textbf{có hoàn
lại}, giữ nguyên kích thước, và lặp lại $B = 2.000$ lần; ở mỗi lần lặp, toàn
bộ chỉ số được tính lại. Phân phối của 2.000 giá trị thu được xấp xỉ phân phối
lấy mẫu của chỉ số, từ đó sử dụng phân vị 2{,}5\% và 97{,}5\% làm khoảng tin
cậy 95\%.

\input{table_bootstrap_ci}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{bootstrap_ci.png}
  \caption{Phân phối bootstrap của năm chỉ số đánh giá; đường liền là ước
  lượng điểm, hai đường đứt là biên khoảng tin cậy 95\%.}
  \label{fig:bootstrap}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item Trung bình bootstrap trùng khớp với ước lượng điểm tới bốn chữ số thập
        phân ở cả năm chỉ số, cho thấy các ước lượng \textbf{không có dấu hiệu
        bị chệch} đáng kể.
  \item \textbf{Accuracy} và \textbf{ROC-AUC} là hai chỉ số ổn định nhất; độ
        rộng khoảng tin cậy lần lượt là $0.0211$ và $0.0184$, do được tính trên
        toàn bộ 4.363 bản ghi.
  \item \textbf{Precision} và \textbf{recall} của lớp At-risk dao động mạnh
        gấp đôi, với độ rộng $0.0407$ và $0.0421$; điều này dễ hiểu vì chỉ dựa trên
        nhóm thiểu số 1.559 sinh viên At-risk.
\end{itemize}

Hệ quả quan trọng nhất của phân tích này liên quan đến việc \textbf{xếp hạng
các mô hình} ở Mục~\ref{sec:compare_full}. Khoảng tin cậy của F1 trải từ
$0.7692$ đến $0.8012$, tức rộng $0.032$, trong khi chênh lệch F1 giữa ba mô
hình cây chỉ nằm trong khoảng $0.0022$ đến $0.0037$. Các chênh lệch này
\textbf{nhỏ hơn nhiều so với sai số lấy mẫu}, nên không đủ cơ sở thống kê để
khẳng định CatBoost thực sự vượt trội hơn LightGBM và Random Forest. Ba mô
hình này nên được xem là \textbf{tương đương} về hiệu suất, còn việc CatBoost
dẫn đầu có thể phản ánh dao động riêng của tập kiểm tra. Ngược lại, khoảng
cách giữa Logistic Regression, $0.7591$, và nhóm mô hình cây lớn hơn đáng kể,
nên có thể kết luận rằng mô hình tuyến tính có hiệu suất kém hơn.


\section{Diễn giải mô hình bằng SHAP}

Để làm rõ cơ chế ra quyết định của mô hình, phương pháp \textbf{SHAP} được áp
dụng cho \textbf{CatBoost}, mô hình có F1 At-risk cao nhất trên bộ đặc trưng
đầy đủ. Cơ sở lý thuyết của phương pháp này đã được trình bày ở
Mục~\ref{sec:shap_theory}. Phân tích được thực hiện trên cả ba bộ đặc trưng
Full, Top-10 và Top-5 nhằm kiểm tra mức độ nhất quán của diễn giải khi số lượng
đặc trưng thay đổi.

Mục tiêu của phần này không phải thay thế các chỉ số đánh giá ở những mục
trước, mà bổ sung một lớp kiểm tra về khả năng diễn giải của mô hình. Cụ thể,
SHAP được dùng để trả lời ba câu hỏi: mô hình dựa nhiều nhất vào những đặc
trưng nào, các đặc trưng đó đẩy dự báo về phía At-risk hay Pass, và cách mô
hình đưa ra quyết định có còn nhất quán khi rút gọn bộ đặc trưng hay không.
Điều này đặc biệt quan trọng với bài toán cảnh báo sớm, vì kết quả dự báo chỉ
có giá trị thực tiễn khi người dùng có thể liên hệ nó với các tín hiệu học tập
cụ thể.

Trước hết, phân tích được thực hiện ở cấp độ toàn cục nhằm xác định những đặc
trưng có ảnh hưởng lớn nhất đến mô hình. Hình~\ref{fig:shap_full} trình bày kết
quả SHAP trên bộ đặc trưng đầy đủ. Biểu đồ beeswarm cho biết chiều và độ phân
tán ảnh hưởng của từng đặc trưng đối với từng sinh viên, trong khi biểu đồ cột
thể hiện mức độ quan trọng trung bình.
Trong biểu đồ beeswarm, mỗi điểm tương ứng với một sinh viên trong tập kiểm
tra; vị trí trên trục hoành là giá trị SHAP của đặc trưng đó. Điểm nằm về phía
dương làm tăng điểm dự báo At-risk, còn điểm nằm về phía âm kéo dự báo về Pass.
Màu sắc thể hiện giá trị của đặc trưng, trong đó màu đỏ/tím biểu thị giá trị
cao và màu xanh biểu thị giá trị thấp.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.58\textwidth}
    \centering
    \includegraphics[width=\textwidth]{shap_beeswarm_full.png}
    \caption{Beeswarm}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.38\textwidth}
    \centering
    \includegraphics[width=\textwidth]{shap_bar_full.png}
    \caption{Mean $|$SHAP$|$}
  \end{subfigure}
  \caption{Diễn giải SHAP của CatBoost trên bộ đặc trưng đầy đủ gồm 15 đặc
  trưng.}
  \label{fig:shap_full}
\end{figure}

Kết quả SHAP nhất quán cao với thứ hạng độ quan trọng đặc trưng ở
Bảng~\ref{tab:feature_rank}: \code{num\_submitted\_filled},
\code{avg\_score\_filled} và \code{active\_weeks\_filled} là ba đặc trưng có
ảnh hưởng lớn nhất. Chiều tác động cũng dễ diễn giải: giá trị
\code{num\_submitted\_filled} và \code{avg\_score\_filled} \textbf{thấp}, thể
hiện bằng màu xanh, đẩy dự báo về phía \textbf{At-risk} với SHAP dương; trong
khi giá trị \textbf{cao}, thể hiện bằng màu đỏ/tím, kéo dự báo về phía Pass.
Riêng \code{num\_due} thể hiện chiều \textbf{ngược lại}: giá trị cao, màu đỏ,
lại đẩy
về phía At-risk. Điều này hợp lý vì số bài đến hạn cao đồng nghĩa với khối
lượng công việc tích lũy lớn, thường đi kèm nguy cơ trễ hạn hoặc bỏ bài cao
hơn.

Hình~\ref{fig:shap_top10} và Hình~\ref{fig:shap_top5} lặp lại phân tích trên
với hai bộ đặc trưng rút gọn.

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.58\textwidth}
    \centering
    \includegraphics[width=\textwidth]{shap_beeswarm_top10.png}
    \caption{Beeswarm}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.38\textwidth}
    \centering
    \includegraphics[width=\textwidth]{shap_bar_top10.png}
    \caption{Mean $|$SHAP$|$}
  \end{subfigure}
  \caption{Diễn giải SHAP của CatBoost trên bộ đặc trưng Top-10.}
  \label{fig:shap_top10}
\end{figure}

\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.58\textwidth}
    \centering
    \includegraphics[width=\textwidth]{shap_beeswarm_top5.png}
    \caption{Beeswarm}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.38\textwidth}
    \centering
    \includegraphics[width=\textwidth]{shap_bar_top5.png}
    \caption{Mean $|$SHAP$|$}
  \end{subfigure}
  \caption{Diễn giải SHAP của CatBoost trên bộ đặc trưng Top-5.}
  \label{fig:shap_top5}
\end{figure}

Thứ tự và chiều ảnh hưởng của các đặc trưng còn lại hầu như \textbf{không đổi}
khi loại bỏ dần các đặc trưng ít quan trọng. Đây là bằng chứng cho
thấy các đặc trưng bị loại bỏ chủ yếu đóng góp thông tin trùng lặp
hoặc không đáng kể.
Nói cách khác, việc rút gọn đặc trưng không làm thay đổi cơ chế ra quyết định
chính của CatBoost: mô hình vẫn tập trung vào mức độ nộp bài, điểm số, số tuần
hoạt động và khối lượng bài đến hạn. Điều này làm cho bộ Top-5 có ý nghĩa thực
tiễn hơn, vì đây không chỉ là bộ đặc trưng cho hiệu suất gần tương đương, mà
còn giữ được cùng một cách giải thích với bộ đặc trưng đầy đủ.

Sau khi xem xét xu hướng toàn cục, phân tích tiếp tục đi vào cấp độ cục bộ để
làm rõ cách mô hình đưa ra quyết định cho từng sinh viên. Hình~\ref{fig:waterfall}
trình bày biểu đồ waterfall của hai sinh viên đại diện: một trường hợp 
được dự báo mạnh về phía At-risk và một trường hợp được dự báo mạnh về phía Pass.


\begin{figure}[H]
  \centering
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{waterfall_atrisk.png}
    \caption{Trường hợp đẩy mạnh về At-risk}
  \end{subfigure}
  \hfill
  \begin{subfigure}[b]{0.48\textwidth}
    \centering
    \includegraphics[width=\textwidth]{waterfall_pass.png}
    \caption{Trường hợp đẩy mạnh về Pass}
  \end{subfigure}
  \caption{Diễn giải cục bộ bằng waterfall cho hai sinh viên đại diện.}
  \label{fig:waterfall}
\end{figure}

\begin{itemize}[leftmargin=*]
  \item \textbf{Trường hợp At-risk:} sinh viên chưa nộp bài nào, với
        \code{num\_submitted\_filled} $= 0$ và \code{avg\_score\_filled} $= -1$
        là giá trị đánh dấu chưa có điểm, trong khi đã có 9 bài đến hạn
        \code{num\_due} $= 9$. Hai yếu tố này đóng góp lần lượt $+2.60$ và
        $+3.01$ vào điểm số dự báo; cộng thêm số tuần hoạt động thấp
        \code{active\_weeks\_filled} $= 6$, đóng góp $+1.24$, tổng điểm bị
        đẩy lên $8.84$, cao hơn đáng kể so với giá trị nền $0.92$, tương ứng
        với xác suất At-risk rất cao.
  \item \textbf{Trường hợp Pass:} sinh viên có điểm trung bình cao
        \code{avg\_score\_filled} $= 95.33$, kéo điểm số giảm $1.97$, và số
        tuần hoạt động nhiều \code{active\_weeks\_filled} $= 25$, giảm
        $1.17$; cộng thêm lượt truy cập cao
        \code{total\_clicks\_filled} $= 9\,115$, giảm $0.67$ --- tổng hợp
        lại tạo ra điểm số rất thấp ($-5.18$), tương ứng với xác suất Pass rất
        cao.
\end{itemize}

Hai ví dụ trên cho thấy mô hình đưa ra quyết định dựa trên những tín hiệu học
tập trực tiếp: mức độ hoàn thành bài tập và điểm số tích luỹ là những yếu tố
quyết định, thống nhất với phân tích độ quan trọng đặc trưng ở quy mô toàn cục.

% =====================================================================
%  KẾT LUẬN
% =====================================================================
\chapter*{Kết luận}
\addcontentsline{toc}{chapter}{Kết luận}

Từ quá trình tiền xử lý dữ liệu, xây dựng đặc trưng, huấn luyện, so sánh và
diễn giải mô hình, có thể rút ra các kết luận chính sau:

\begin{itemize}[leftmargin=*]
  \item Kết quả theo các mốc dự đoán cho thấy hiệu suất mô hình nhìn chung
        \textbf{tăng dần theo thời gian}, do mô hình quan sát được nhiều tín
        hiệu học tập hơn khi $T$ tiến gần cuối học phần. Tuy nhiên, trong bài
        toán cảnh báo sớm, mốc dự đoán không nên chỉ được chọn theo hiệu suất
        cao nhất: nếu cảnh báo được đưa ra quá muộn, nhà trường còn ít thời
        gian để hỗ trợ sinh viên. Vì vậy, cần chọn mốc cân bằng giữa độ tin cậy
        của dự báo và khoảng thời gian can thiệp còn lại.
  \item Trong số bốn thuật toán thử nghiệm, \textbf{CatBoost} đạt F1 At-risk
        cao nhất trên bộ đặc trưng đầy đủ, đạt 0.785, đồng thời dẫn đầu cả về
        accuracy và precision; LightGBM bám sát ngay phía sau và giữ ROC-AUC
        cao nhất, Random Forest cũng có hiệu suất tương đương. 
        Các mô hình bagging và boosting này vượt trội hơn Logistic Regression
        trên mọi bộ đặc trưng; tuy nhiên, khoảng cách giữa nhóm mô hình này là 
        không lớn và cần được diễn giải thận trọng.
  \item Hiệu suất mô hình có tính ổn định tương đối cao khi giảm số lượng đặc trưng từ
        15 xuống 10 hay thậm chí là 5; thậm chí CatBoost trên bộ Top-10 đạt F1 At-risk cao nhất
        trong toàn bộ thực nghiệm. Các đặc trưng mang tính quyết định đều là đặc trưng hành vi học
        tập, không yêu cầu thông tin nhân khẩu học --- có thể đủ để xây dựng
        một hệ thống cảnh báo sớm gọn nhẹ, dễ triển khai và duy trì.
  \item Phân tích SHAP cho thấy dự báo của mô hình dựa trên những tín hiệu học
        tập trực tiếp, gồm mức độ hoàn thành bài tập, điểm số tích luỹ và khối
        lượng công việc còn tồn đọng. Các diễn giải này giữ được
        tính nhất quán qua cả ba bộ đặc trưng.
\end{itemize}

\textbf{Hạn chế và hướng phát triển.} Bên cạnh các kết quả đạt được, nghiên
cứu vẫn còn một số giới hạn cần được xem xét trước khi mở rộng thành hệ thống
cảnh báo sớm trong thực tế:

\begin{itemize}[leftmargin=*]
  \item \textbf{Phạm vi đánh giá theo thời gian.} Phần thực nghiệm chuyên sâu
        hiện tập trung tại mốc 150 ngày. Đây là mốc đủ giàu thông tin để phân
        tích mô hình, nhưng chưa trả lời đầy đủ câu hỏi cảnh báo có thể được
        đưa ra sớm đến mức nào. Hướng phát triển tiếp theo là huấn luyện, tinh
        chỉnh và so sánh mô hình tại nhiều mốc dự đoán khác nhau, đồng thời
        theo dõi cách xác suất rủi ro của cùng một sinh viên thay đổi theo tiến
        trình học tập.
  \item \textbf{Lựa chọn ngưỡng vận hành.} Các phân tích hiệu chỉnh ngưỡng trong
        báo cáo giúp minh hoạ sự đánh đổi giữa precision và recall, nhưng ngưỡng
        cuối cùng vẫn cần được lựa chọn bằng kiểm định chéo hoặc tập validation
        trên dữ liệu huấn luyện. Khi triển khai, ngưỡng nên được cố định trước,
        sau đó đánh giá lại trên một tập kiểm tra độc lập và được điều chỉnh
        theo chi phí thực tế giữa bỏ sót sinh viên At-risk và tạo cảnh báo nhầm.
  \item \textbf{Khả năng khái quát của mô hình.} Kết quả hiện được xây dựng trên
        bộ dữ liệu OULAD và phản ánh bối cảnh đào tạo cụ thể của bộ dữ liệu này.
        Vì vậy, cần kiểm tra mô hình trên các khóa học, học kỳ hoặc cơ sở đào
        tạo khác để đánh giá độ ổn định khi phân phối dữ liệu thay đổi. Việc
        đánh giá ngoài mẫu cũng giúp xác định liệu các đặc trưng hành vi học
        tập quan trọng có giữ vai trò tương tự trong những bối cảnh khác hay
        không.
  \item \textbf{Triển khai và sử dụng kết quả dự báo.} Một hệ thống cảnh báo sớm
        không chỉ cần mô hình có hiệu suất tốt, mà còn cần quy trình sử dụng rõ
        ràng cho giảng viên và cố vấn học tập. Do đó, các bước tiếp theo nên bao
        gồm thiết kế cách trình bày lý do cảnh báo, xác định mức ưu tiên can
        thiệp, theo dõi hiệu quả hỗ trợ sau cảnh báo và giám sát hiện tượng suy
        giảm hiệu suất mô hình khi dữ liệu mới phát sinh.
\end{itemize}

% =====================================================================
%  TÀI LIỆU THAM KHẢO
% =====================================================================
\clearpage
\renewcommand{\bibname}{Tài liệu tham khảo}
\addcontentsline{toc}{chapter}{Tài liệu tham khảo}

\begin{thebibliography}{99}
  \bibitem{oulad_dataset}
  Tài liệu về bộ dữ liệu Open University Learning Analytics Dataset. Điền thông
  tin tác giả, năm xuất bản, tiêu đề đầy đủ và liên kết truy cập tại đây.

  \bibitem{learning_analytics}
  Tài liệu tổng quan về learning analytics và educational data mining. Điền
  thông tin trích dẫn đầy đủ tại đây.

  \bibitem{logistic_regression}
  Tài liệu về Logistic Regression và phân loại nhị phân. Điền thông tin trích
  dẫn đầy đủ tại đây.

  \bibitem{random_forest}
  Tài liệu về Decision Tree, Random Forest và độ quan trọng đặc trưng dựa trên
  Gini. Điền thông tin trích dẫn đầy đủ tại đây.

  \bibitem{catboost}
  Tài liệu về CatBoost và mô hình gradient boosting trên đặc trưng phân loại.
  Điền thông tin trích dẫn đầy đủ tại đây.

  \bibitem{lightgbm}
  Tài liệu về LightGBM và chiến lược phát triển cây leaf-wise. Điền thông tin
  trích dẫn đầy đủ tại đây.

  \bibitem{smote}
  Tài liệu về SMOTE và các phương pháp xử lý mất cân bằng nhãn. Điền thông tin
  trích dẫn đầy đủ tại đây.

  \bibitem{optuna_tpe}
  Tài liệu về Bayesian Optimization, Optuna và Tree-structured Parzen Estimator.
  Điền thông tin trích dẫn đầy đủ tại đây.

  \bibitem{shap}
  Tài liệu về SHAP, giá trị Shapley và TreeSHAP. Điền thông tin trích dẫn đầy đủ
  tại đây.
\end{thebibliography}

\end{document}
